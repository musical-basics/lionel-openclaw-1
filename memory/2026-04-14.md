# 2026-04-14

- In `dreamplay-media-indexer-2`, implemented two DreamPlay Assets features and pushed them to `main` at `0043b03`: reveal-in-Finder local cache backed by Supabase + R2, and browser upload via presigned R2 URL plus background processing.
- Set up the processing worker on the OpenClaw machine itself using workspace-local tools under `.worker-tools` (`pm2`, `ffmpeg-static`, `ffprobe-static`). Launcher: `.worker-tools/run-dreamplay-processor.sh`. PM2 process name: `dreamplay-processor`.
- Critical machine-local worker env fix: `.env.local` on this Linux box must use workspace paths, not Lionel's old Mac paths. Working values are:
  - `ASSETS_ROOT=/home/openclaw/.openclaw/workspace/dreamplay-media-indexer-2/.asset-cache`
  - `LOCAL_WORKSPACE_DIR=/home/openclaw/.openclaw/workspace/dreamplay-media-indexer-2/.asset-cache`
  - `CATALOG_DB_PATH=/home/openclaw/.openclaw/workspace/dreamplay-media-indexer-2/.indexer-cache/catalog.db`
  - `THUMBS_DIR=/home/openclaw/.openclaw/workspace/dreamplay-media-indexer-2/.indexer-cache/thumbs`
- Browser uploads were initially blocked by Cloudflare R2 bucket CORS, not by the worker. Required allowing browser `PUT` uploads from `https://dreamplay-media-indexer.vercel.app` and `http://localhost:3001`.
- End-to-end upload/processing was confirmed on asset `41d16ebb-88b2-4dca-bce3-fa28787f3013`; it received a non-empty `aiDescription` and an R2 `thumbPath`.
- Claude Code auth succeeded, but OpenClaw ACP spawn had local harness issues (`claude-code` was the wrong agent id; `claude` is the built-in alias, and local PATH/cache wiring was also problematic). Direct local Claude/acpx audit was used as fallback.
- Claude's audit found and fixed two real issues, then the fix was verified with `pnpm build` and pushed to `main` at `f7a21ff`: `application/mxf` had been misclassified as an image in upload routes, and `fileSize` was missing from destructuring in `/api/upload/presign`. README architecture notes were also updated.
- Lionel wants Claude Code runs launched with model `sonnet` and effort level `high` by default.
- Lionel provided a Claude Code CLI reference for this machine. Key operational takeaway: for direct scripted use on this box, prefer `/home/openclaw/.openclaw/workspace/.npm-global/bin/claude -p ...` with explicit flags rather than relying only on ACP bridge behavior.
- Lionel wants non-trivial tasks delegated to a subagent by default so the main chat stays responsive while work continues in the background.
- Lionel confirmed Manus has already been canceled, so it should no longer be treated as an unresolved admin item.
- DreamPlay keyboard plan: send DS 6.0 to China with his cofounder so the factory can reverse-engineer narrow keys for the DreamPlay 1 prototype, targeting a $999 retail price.
- To avoid a roughly $32K custom narrow-key build, the current DreamPlay 1 prototype plan is to reuse an existing Steinbuhler/Kawai key mechanism instead of building keys entirely from scratch.
- A replacement DS 6.0 from David Steinbuhler would cost about $7K to $8K total with about a 6-week turnaround, but Lionel is not ordering it yet because of cash constraints.
- Lionel expects to be without a practice keyboard for about 2 months, which is acceptable because he is traveling to Maryland anyway.
- Maryland trip decision is now made: fly to Maryland on April 15, with a 4 PM EDT flight.
- Masterclass launch direction: use the simple 2-section landing page instead of the 5-section version.
- A $1 VIP waitlist already exists for the masterclass launch.
- The intro video edit is currently blocked by an assistant-made change Lionel cannot resolve; priority is to ship the waitlist page before touching the video.
- Current masterclass/DreamPlay sequencing rule: no additional DreamPlay spending until the masterclass is generating revenue.
- Immediate action items Lionel listed: message the assistant tonight to fix the video edit, ship the 2-section waitlist page from Maryland, and package the DS 6.0 properly before his cofounder takes it to China.
