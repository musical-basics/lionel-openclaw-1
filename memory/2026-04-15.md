# 2026-04-15

- Enabled `memory-lancedb` in OpenClaw config and verified the plugin loads, but real recall/search failed because the configured OpenAI embeddings key returned a 401 invalid API key error.
- `ltm stats` showed 0 memories in the LanceDB store, so even with a valid key it would still need time or manual seeding to become useful.
- Lionel decided to check the LanceDB setup later rather than continue now.
- Important operational note: until a valid embeddings key is configured or the memory slot is switched back, `memory-lancedb` remains the active memory plugin but is not providing useful recall.
