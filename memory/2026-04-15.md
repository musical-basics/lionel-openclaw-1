# 2026-04-15

- Enabled `memory-lancedb` in OpenClaw config and verified the plugin loads, but real recall/search failed because the configured OpenAI embeddings key returned a 401 invalid API key error.
- `ltm stats` showed 0 memories in the LanceDB store, so even with a valid key it would still need time or manual seeding to become useful.
- Lionel decided to check the LanceDB setup later rather than continue now.
- Important operational note: until a valid embeddings key is configured or the memory slot is switched back, `memory-lancedb` remains the active memory plugin but is not providing useful recall.
- Switched the active memory slot back to `memory-core` and disabled `memory-lancedb` in config for now.
- Key architecture note: in stock OpenClaw, the active memory backend is chosen by the exclusive `plugins.slots.memory` slot, so `memory-core` and `memory-lancedb` are not both active at the same time. If Lionel wants regular text memory plus semantics later, the cleaner path is likely `memory-core` with embeddings rather than replacing it with LanceDB.
- Later, `~/.openclaw/openclaw.json` was updated to the cleaner setup: `plugins.slots.memory = memory-core`, `models.providers.openai.apiKey = "${OPENAI_API_KEY}"`, and `agents.defaults.memorySearch` set to OpenAI `text-embedding-3-small`.
- Verification showed the new setup is structurally correct, but embeddings are still not usable because OpenAI now returns `429 insufficient_quota` rather than the earlier invalid-key `401`. So the key wiring works, but billing/quota is the current blocker.
