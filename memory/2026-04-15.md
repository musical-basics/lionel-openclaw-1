# 2026-04-15

- Enabled `memory-lancedb` in OpenClaw config and verified the plugin loads, but real recall/search failed because the configured OpenAI embeddings key returned a 401 invalid API key error.
- `ltm stats` showed 0 memories in the LanceDB store, so even with a valid key it would still need time or manual seeding to become useful.
- Lionel decided to check the LanceDB setup later rather than continue now.
- Important operational note: until a valid embeddings key is configured or the memory slot is switched back, `memory-lancedb` remains the active memory plugin but is not providing useful recall.
- Switched the active memory slot back to `memory-core` and disabled `memory-lancedb` in config for now.
- Key architecture note: in stock OpenClaw, the active memory backend is chosen by the exclusive `plugins.slots.memory` slot, so `memory-core` and `memory-lancedb` are not both active at the same time. If Lionel wants regular text memory plus semantics later, the cleaner path is likely `memory-core` with embeddings rather than replacing it with LanceDB.
- Later, `~/.openclaw/openclaw.json` was updated to the cleaner setup: `plugins.slots.memory = memory-core`, `models.providers.openai.apiKey = "${OPENAI_API_KEY}"`, and `agents.defaults.memorySearch` set to OpenAI `text-embedding-3-small`.
- Verification showed the new setup is structurally correct, but embeddings were initially not usable because OpenAI returned `429 insufficient_quota` rather than the earlier invalid-key `401`. That confirmed the key wiring worked and billing/quota was the blocker.
- After Lionel added funds, `openclaw memory status --deep --index` succeeded: `memory-core` is active, provider `openai`, model `text-embedding-3-small`, embeddings are ready, 12/12 memory files indexed, and the embedding cache populated successfully.
- Follow-up `openclaw memory search` probes returned relevant hits from recent memory files, confirming semantic memory search is now working on the regular text memory backend.
- Lionel shared a proposed v1 orchestration spec: this OpenClaw agent as "Command" / chief of staff over three Hermes agents (Marketing, Development, Operations), with Claude Code reserved for complex coding and Telegram DMs as the current dispatch path.
- Initial assessment: the architecture is directionally strong, especially centralized context plus specialized workers, but it needs a durable task ledger / handoff schema so Telegram chat threads do not become the system of record.
- Drafted `HERMES-ORCHESTRATION-V2.md` in the workspace. Core recommendation: Command owns truth/context, Hermes agents are stateless workers, and TaskFlow becomes the durable control plane instead of Telegram threads.
