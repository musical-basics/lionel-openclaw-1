# 2026-04-16

- `memory-core` semantic memory is now working with OpenAI embeddings on this OpenClaw instance. The active setup is `plugins.slots.memory = memory-core` plus `agents.defaults.memorySearch.provider = openai` and `model = text-embedding-3-small`, with the OpenAI key resolved from `${OPENAI_API_KEY}`. After billing was added, deep status/index succeeded and semantic searches returned relevant hits from recent memory files.
- Important memory model note for future explanations: in `memory-core`, the Markdown memory files are the source of truth and embeddings are only a semantic index over chunks of those files, not a separate store of the real memory.
- Lionel approved the high-level multi-agent direction: keep Command/OpenClaw as the orchestrator with TaskFlow and memory on one VPS, and likely consolidate Hermes 1/2/3 onto a separate worker VPS behind a single Hermes gateway. Recommended long-term link is Tailscale-only HTTP, with TaskFlow remaining canonical on Command and Hermes treated as mostly stateless workers.
- Draft architecture docs were created in the workspace: `HERMES-ORCHESTRATION-V2.md`, `HERMES-GATEWAY-API-V1.md`, `specs/hermes-gateway-v1.openapi.yaml`, and `specs/hermes-task.v1.schema.json`.
- A sanitized export of the setup was pushed to GitHub repo `musical-basics/lionel-openclaw-1` on branch `setup-export`. Raw local history was intentionally not pushed because the workspace repo contains private memory/history and secrets/runtime state that should not be published as-is.
- Hermes connection test status: SSH connectivity to the candidate Hermes server succeeded, but the provided root login failed at authentication. Durable takeaway is that network reachability is fine, but valid credentials or the correct SSH user are still needed before remote setup work can proceed.
- SSH access to Bot 2 works with `root@5.161.102.170` and the provided password. The live service is `hermes-gateway.service`, running as user `hermes`, with `ExecStart=/home/hermes/.local/bin/hermes gateway run` and working directory `/home/hermes`. Likely code/config locations are under `/home/hermes/.hermes/hermes-agent`, with config at `/home/hermes/.hermes/config.yaml` and environment file `/home/hermes/.hermes/.env`.
- Installed Tailscale on Bot 2 (`5.161.102.170`) successfully and enabled `tailscaled`. It was then authenticated into Lionel's tailnet successfully. Verified Tailscale IP is `100.95.180.106` (`ubuntu-4gb-ash-2`), and the current OpenClaw host can ping it over Tailscale. This clears the network prerequisite for converting the box from Discord-bot mode toward Hermes worker-gateway mode.
- Set up a parallel Hermes worker gateway on Bot 2 without touching the live Discord bot. Created Hermes profiles `marketing`, `development`, and `operations`, copied the existing auth store into them, replaced their cloned kawaii display personality with concise behavior, and installed role-specific `SOUL.md` files.
- Deployed and started a new systemd service `hermes-worker-gateway.service` on Bot 2. It runs `/home/hermes/worker-gateway/worker_gateway.py` as user `hermes`, binds to the box's Tailscale IP, and exposes `GET /v1/health`, `POST /v1/tasks`, `GET /v1/tasks/{id}`, and `POST /v1/tasks/{id}/cancel`.
- Verified the new worker gateway end-to-end over Tailscale from this OpenClaw host: health returned all three workers ready, task creation worked with bearer auth, polling worked, and a sample marketing task executed successfully through the `marketing` Hermes profile and returned structured `needs_review` output.
- Wrote `HERMES-HYBRID-SETUP-RUNBOOK.md` to document the whole hybrid setup process end-to-end: architecture, what was changed on Bot 2, live services/paths, API surface, verification steps, current limitations, and the remaining Command-side integration work. This doc is intended for the sanitized git export branch.
- Added a first Command-side Hermes bridge in the OpenClaw workspace: `skills/hermes-dispatch/SKILL.md` plus `skills/hermes-dispatch/scripts/hermes_gateway_client.py`. The skill now loads in OpenClaw and the client can reach the live Bot 2 gateway over Tailscale via `health`, confirming all three workers are ready from the Command host side. Full task submission is still blocked on copying the bearer token into a local untracked `.env.hermes-gateway` file because remote Bot 2 SSH auth was not available in this session.
# 2026-04-16

- `memory-core` semantic memory is now working with OpenAI embeddings on this OpenClaw instance. The active setup is `plugins.slots.memory = memory-core` plus `agents.defaults.memorySearch.provider = openai` and `model = text-embedding-3-small`, with the OpenAI key resolved from `${OPENAI_API_KEY}`. After billing was added, deep status/index succeeded and semantic searches returned relevant hits from recent memory files.
- Important memory model note for future explanations: in `memory-core`, the Markdown memory files are the source of truth and embeddings are only a semantic index over chunks of those files, not a separate store of the real memory.
- Lionel approved the high-level multi-agent direction: keep Command/OpenClaw as the orchestrator with TaskFlow and memory on one VPS, and likely consolidate Hermes 1/2/3 onto a separate worker VPS behind a single Hermes gateway. Recommended long-term link is Tailscale-only HTTP, with TaskFlow remaining canonical on Command and Hermes treated as mostly stateless workers.
- Draft architecture docs were created in the workspace: `HERMES-ORCHESTRATION-V2.md`, `HERMES-GATEWAY-API-V1.md`, `specs/hermes-gateway-v1.openapi.yaml`, and `specs/hermes-task.v1.schema.json`.
- A sanitized export of the setup was pushed to GitHub repo `musical-basics/lionel-openclaw-1` on branch `setup-export`. Raw local history was intentionally not pushed because the workspace repo contains private memory/history and secrets/runtime state that should not be published as-is.
- Hermes connection test status: SSH connectivity to the candidate Hermes server succeeded, but the provided root login failed at authentication. Durable takeaway is that network reachability is fine, but valid credentials or the correct SSH user are still needed before remote setup work can proceed.
- SSH access to Bot 2 works with `root@5.161.102.170` and the provided password. The live service is `hermes-gateway.service`, running as user `hermes`, with `ExecStart=/home/hermes/.local/bin/hermes gateway run` and working directory `/home/hermes`. Likely code/config locations are under `/home/hermes/.hermes/hermes-agent`, with config at `/home/hermes/.hermes/config.yaml` and environment file `/home/hermes/.hermes/.env`.
- Installed Tailscale on Bot 2 (`5.161.102.170`) successfully and enabled `tailscaled`. It was then authenticated into Lionel's tailnet successfully. Verified Tailscale IP is `100.95.180.106` (`ubuntu-4gb-ash-2`), and the current OpenClaw host can ping it over Tailscale. This clears the network prerequisite for converting the box from Discord-bot mode toward Hermes worker-gateway mode.
- Set up a parallel Hermes worker gateway on Bot 2 without touching the live Discord bot. Created Hermes profiles `marketing`, `development`, and `operations`, copied the existing auth store into them, replaced their cloned kawaii display personality with concise behavior, and installed role-specific `SOUL.md` files.
- Deployed and started a new systemd service `hermes-worker-gateway.service` on Bot 2. It runs `/home/hermes/worker-gateway/worker_gateway.py` as user `hermes`, binds to the box's Tailscale IP, and exposes `GET /v1/health`, `POST /v1/tasks`, `GET /v1/tasks/{id}`, and `POST /v1/tasks/{id}/cancel`.
- Verified the new worker gateway end-to-end over Tailscale from this OpenClaw host: health returned all three workers ready, task creation worked with bearer auth, polling worked, and a sample marketing task executed successfully through the `marketing` Hermes profile and returned structured `needs_review` output.
- Wrote `HERMES-HYBRID-SETUP-RUNBOOK.md` to document the whole hybrid setup process end-to-end: architecture, what was changed on Bot 2, live services/paths, API surface, verification steps, current limitations, and the remaining Command-side integration work. This doc is intended for the sanitized git export branch.
- Added a first Command-side Hermes bridge in the OpenClaw workspace: `skills/hermes-dispatch/SKILL.md` plus `skills/hermes-dispatch/scripts/hermes_gateway_client.py`. The skill now loads in OpenClaw and the client can reach the live Bot 2 gateway over Tailscale via `health`, confirming all three workers are ready from the Command host side. Full task submission is still blocked on copying the bearer token into a local untracked `.env.hermes-gateway` file because remote Bot 2 SSH auth was not available in this session.
- Important user-facing mental model for future explanations: this setup is **3 logical Hermes workers behind 1 gateway service on 1 VPS**, not three separate gateway services. The workers are `marketing`, `development`, and `operations`. The old Discord Hermes bot is still running separately on the same box and is not part of the new worker-gateway model.
- The sanitized `setup-export` branch was updated again to include both the new runbook and the initial Command-side bridge artifacts. Current bridge state is partial: tailnet health checks work from Command to Bot 2, but real task submission still needs the bearer token on the Command box. Useful operational detail: `/v1/health` is reachable without auth on the tailnet, while task endpoints still require bearer auth.
- Lionel then chose the simpler same-VPS direction for the worker trio on the Command box itself, while explicitly keeping the remote Bot 2 worker stack untouched for separate management.
- On this VPS, three local isolated OpenClaw agents were created for the same worker roles: `marketing`, `development`, and `operations`. Their workspaces are `/home/openclaw/.openclaw/workspace-marketing`, `/home/openclaw/.openclaw/workspace-development`, and `/home/openclaw/.openclaw/workspace-operations`, with matching agent state under `/home/openclaw/.openclaw/agents/<id>/agent`.
- Important architecture nuance for future explanations: the local same-VPS version is not the separate Hermes CLI runtime. It is a Hermes-equivalent 3-worker split implemented natively as isolated OpenClaw agents on the same host, which is simpler and better for fast local iteration.
- Each local worker was given role-specific `SOUL.md`, `AGENTS.md`, and related workspace files so they behave like already-initialized specialists instead of first-run assistants.
- Verified all three local workers respond through the real OpenClaw path with `openclaw agent --agent <worker> --message ... --json`, and added a reusable local bridge skill/client: `skills/hermes-local-dispatch/SKILL.md` and `skills/hermes-local-dispatch/scripts/hermes_local_client.py`.
- Wrote `HERMES-SAME-VPS-ARCHITECTURE.md` as the canonical top-level doc for this local-worker architecture. Current recommended rule is: use local same-VPS workers first for work that can stay on this box; keep the remote Bot 2 stack as an optional separate worker environment.
# 2026-04-16

- `memory-core` semantic memory is now working with OpenAI embeddings on this OpenClaw instance. The active setup is `plugins.slots.memory = memory-core` plus `agents.defaults.memorySearch.provider = openai` and `model = text-embedding-3-small`, with the OpenAI key resolved from `${OPENAI_API_KEY}`. After billing was added, deep status/index succeeded and semantic searches returned relevant hits from recent memory files.
- Important memory model note for future explanations: in `memory-core`, the Markdown memory files are the source of truth and embeddings are only a semantic index over chunks of those files, not a separate store of the real memory.
- Lionel approved the high-level multi-agent direction: keep Command/OpenClaw as the orchestrator with TaskFlow and memory on one VPS, and likely consolidate Hermes 1/2/3 onto a separate worker VPS behind a single Hermes gateway. Recommended long-term link is Tailscale-only HTTP, with TaskFlow remaining canonical on Command and Hermes treated as mostly stateless workers.
- Draft architecture docs were created in the workspace: `HERMES-ORCHESTRATION-V2.md`, `HERMES-GATEWAY-API-V1.md`, `specs/hermes-gateway-v1.openapi.yaml`, and `specs/hermes-task.v1.schema.json`.
- A sanitized export of the setup was pushed to GitHub repo `musical-basics/lionel-openclaw-1` on branch `setup-export`. Raw local history was intentionally not pushed because the workspace repo contains private memory/history and secrets/runtime state that should not be published as-is.
- Hermes connection test status: SSH connectivity to the candidate Hermes server succeeded, but the provided root login failed at authentication. Durable takeaway is that network reachability is fine, but valid credentials or the correct SSH user are still needed before remote setup work can proceed.
- SSH access to Bot 2 works with `root@5.161.102.170` and the provided password. The live service is `hermes-gateway.service`, running as user `hermes`, with `ExecStart=/home/hermes/.local/bin/hermes gateway run` and working directory `/home/hermes`. Likely code/config locations are under `/home/hermes/.hermes/hermes-agent`, with config at `/home/hermes/.hermes/config.yaml` and environment file `/home/hermes/.hermes/.env`.
- Installed Tailscale on Bot 2 (`5.161.102.170`) successfully and enabled `tailscaled`. It was then authenticated into Lionel's tailnet successfully. Verified Tailscale IP is `100.95.180.106` (`ubuntu-4gb-ash-2`), and the current OpenClaw host can ping it over Tailscale. This clears the network prerequisite for converting the box from Discord-bot mode toward Hermes worker-gateway mode.
- Set up a parallel Hermes worker gateway on Bot 2 without touching the live Discord bot. Created Hermes profiles `marketing`, `development`, and `operations`, copied the existing auth store into them, replaced their cloned kawaii display personality with concise behavior, and installed role-specific `SOUL.md` files.
- Deployed and started a new systemd service `hermes-worker-gateway.service` on Bot 2. It runs `/home/hermes/worker-gateway/worker_gateway.py` as user `hermes`, binds to the box's Tailscale IP, and exposes `GET /v1/health`, `POST /v1/tasks`, `GET /v1/tasks/{id}`, and `POST /v1/tasks/{id}/cancel`.
- Verified the new worker gateway end-to-end over Tailscale from this OpenClaw host: health returned all three workers ready, task creation worked with bearer auth, polling worked, and a sample marketing task executed successfully through the `marketing` Hermes profile and returned structured `needs_review` output.
- Wrote `HERMES-HYBRID-SETUP-RUNBOOK.md` to document the whole hybrid setup process end-to-end: architecture, what was changed on Bot 2, live services/paths, API surface, verification steps, current limitations, and the remaining Command-side integration work. This doc is intended for the sanitized git export branch.
- Added a first Command-side Hermes bridge in the OpenClaw workspace: `skills/hermes-dispatch/SKILL.md` plus `skills/hermes-dispatch/scripts/hermes_gateway_client.py`. The skill now loads in OpenClaw and the client can reach the live Bot 2 gateway over Tailscale via `health`, confirming all three workers are ready from the Command host side. Full task submission is still blocked on copying the bearer token into a local untracked `.env.hermes-gateway` file because remote Bot 2 SSH auth was not available in this session.
# 2026-04-16

- `memory-core` semantic memory is now working with OpenAI embeddings on this OpenClaw instance. The active setup is `plugins.slots.memory = memory-core` plus `agents.defaults.memorySearch.provider = openai` and `model = text-embedding-3-small`, with the OpenAI key resolved from `${OPENAI_API_KEY}`. After billing was added, deep status/index succeeded and semantic searches returned relevant hits from recent memory files.
- Important memory model note for future explanations: in `memory-core`, the Markdown memory files are the source of truth and embeddings are only a semantic index over chunks of those files, not a separate store of the real memory.
- Lionel approved the high-level multi-agent direction: keep Command/OpenClaw as the orchestrator with TaskFlow and memory on one VPS, and likely consolidate Hermes 1/2/3 onto a separate worker VPS behind a single Hermes gateway. Recommended long-term link is Tailscale-only HTTP, with TaskFlow remaining canonical on Command and Hermes treated as mostly stateless workers.
- Draft architecture docs were created in the workspace: `HERMES-ORCHESTRATION-V2.md`, `HERMES-GATEWAY-API-V1.md`, `specs/hermes-gateway-v1.openapi.yaml`, and `specs/hermes-task.v1.schema.json`.
- A sanitized export of the setup was pushed to GitHub repo `musical-basics/lionel-openclaw-1` on branch `setup-export`. Raw local history was intentionally not pushed because the workspace repo contains private memory/history and secrets/runtime state that should not be published as-is.
- Hermes connection test status: SSH connectivity to the candidate Hermes server succeeded, but the provided root login failed at authentication. Durable takeaway is that network reachability is fine, but valid credentials or the correct SSH user are still needed before remote setup work can proceed.
- SSH access to Bot 2 works with `root@5.161.102.170` and the provided password. The live service is `hermes-gateway.service`, running as user `hermes`, with `ExecStart=/home/hermes/.local/bin/hermes gateway run` and working directory `/home/hermes`. Likely code/config locations are under `/home/hermes/.hermes/hermes-agent`, with config at `/home/hermes/.hermes/config.yaml` and environment file `/home/hermes/.hermes/.env`.
- Installed Tailscale on Bot 2 (`5.161.102.170`) successfully and enabled `tailscaled`. It was then authenticated into Lionel's tailnet successfully. Verified Tailscale IP is `100.95.180.106` (`ubuntu-4gb-ash-2`), and the current OpenClaw host can ping it over Tailscale. This clears the network prerequisite for converting the box from Discord-bot mode toward Hermes worker-gateway mode.
- Set up a parallel Hermes worker gateway on Bot 2 without touching the live Discord bot. Created Hermes profiles `marketing`, `development`, and `operations`, copied the existing auth store into them, replaced their cloned kawaii display personality with concise behavior, and installed role-specific `SOUL.md` files.
- Deployed and started a new systemd service `hermes-worker-gateway.service` on Bot 2. It runs `/home/hermes/worker-gateway/worker_gateway.py` as user `hermes`, binds to the box's Tailscale IP, and exposes `GET /v1/health`, `POST /v1/tasks`, `GET /v1/tasks/{id}`, and `POST /v1/tasks/{id}/cancel`.
- Verified the new worker gateway end-to-end over Tailscale from this OpenClaw host: health returned all three workers ready, task creation worked with bearer auth, polling worked, and a sample marketing task executed successfully through the `marketing` Hermes profile and returned structured `needs_review` output.
- Wrote `HERMES-HYBRID-SETUP-RUNBOOK.md` to document the whole hybrid setup process end-to-end: architecture, what was changed on Bot 2, live services/paths, API surface, verification steps, current limitations, and the remaining Command-side integration work. This doc is intended for the sanitized git export branch.
- Added a first Command-side Hermes bridge in the OpenClaw workspace: `skills/hermes-dispatch/SKILL.md` plus `skills/hermes-dispatch/scripts/hermes_gateway_client.py`. The skill now loads in OpenClaw and the client can reach the live Bot 2 gateway over Tailscale via `health`, confirming all three workers are ready from the Command host side. Full task submission is still blocked on copying the bearer token into a local untracked `.env.hermes-gateway` file because remote Bot 2 SSH auth was not available in this session.
- Important user-facing mental model for future explanations: this setup is **3 logical Hermes workers behind 1 gateway service on 1 VPS**, not three separate gateway services. The workers are `marketing`, `development`, and `operations`. The old Discord Hermes bot is still running separately on the same box and is not part of the new worker-gateway model.
- The sanitized `setup-export` branch was updated again to include both the new runbook and the initial Command-side bridge artifacts. Current bridge state is partial: tailnet health checks work from Command to Bot 2, but real task submission still needs the bearer token on the Command box. Useful operational detail: `/v1/health` is reachable without auth on the tailnet, while task endpoints still require bearer auth.
- Lionel then chose the simpler same-VPS direction for the worker trio on the Command box itself, while explicitly keeping the remote Bot 2 worker stack untouched for separate management.
- On this VPS, three local isolated OpenClaw agents were created for the same worker roles: `marketing`, `development`, and `operations`. Their workspaces are `/home/openclaw/.openclaw/workspace-marketing`, `/home/openclaw/.openclaw/workspace-development`, and `/home/openclaw/.openclaw/workspace-operations`, with matching agent state under `/home/openclaw/.openclaw/agents/<id>/agent`.
- Important architecture nuance for future explanations: the local same-VPS version is not the separate Hermes CLI runtime. It is a Hermes-equivalent 3-worker split implemented natively as isolated OpenClaw agents on the same host, which is simpler and better for fast local iteration.
- Each local worker was given role-specific `SOUL.md`, `AGENTS.md`, and related workspace files so they behave like already-initialized specialists instead of first-run assistants.
- Verified all three local workers respond through the real OpenClaw path with `openclaw agent --agent <worker> --message ... --json`, and added a reusable local bridge skill/client: `skills/hermes-local-dispatch/SKILL.md` and `skills/hermes-local-dispatch/scripts/hermes_local_client.py`.
- Wrote `HERMES-SAME-VPS-ARCHITECTURE.md` as the canonical top-level doc for this local-worker architecture. Current recommended rule is: use local same-VPS workers first for work that can stay on this box; keep the remote Bot 2 stack as an optional separate worker environment.
- During follow-on integration, the clean TaskFlow path for the local same-VPS workers was identified as OpenClaw's built-in `webhooks` plugin bound to this owner session, with a local route that can create/manage TaskFlows around worker dispatch. Important limitation discovered: that bridge only loads on gateway startup, so enabling it requires a local gateway restart on this VPS. Remote Bot 2 remains unaffected by that plan.
# 2026-04-16

- `memory-core` semantic memory is now working with OpenAI embeddings on this OpenClaw instance. The active setup is `plugins.slots.memory = memory-core` plus `agents.defaults.memorySearch.provider = openai` and `model = text-embedding-3-small`, with the OpenAI key resolved from `${OPENAI_API_KEY}`. After billing was added, deep status/index succeeded and semantic searches returned relevant hits from recent memory files.
- Important memory model note for future explanations: in `memory-core`, the Markdown memory files are the source of truth and embeddings are only a semantic index over chunks of those files, not a separate store of the real memory.
- Lionel approved the high-level multi-agent direction: keep Command/OpenClaw as the orchestrator with TaskFlow and memory on one VPS, and likely consolidate Hermes 1/2/3 onto a separate worker VPS behind a single Hermes gateway. Recommended long-term link is Tailscale-only HTTP, with TaskFlow remaining canonical on Command and Hermes treated as mostly stateless workers.
- Draft architecture docs were created in the workspace: `HERMES-ORCHESTRATION-V2.md`, `HERMES-GATEWAY-API-V1.md`, `specs/hermes-gateway-v1.openapi.yaml`, and `specs/hermes-task.v1.schema.json`.
- A sanitized export of the setup was pushed to GitHub repo `musical-basics/lionel-openclaw-1` on branch `setup-export`. Raw local history was intentionally not pushed because the workspace repo contains private memory/history and secrets/runtime state that should not be published as-is.
- Hermes connection test status: SSH connectivity to the candidate Hermes server succeeded, but the provided root login failed at authentication. Durable takeaway is that network reachability is fine, but valid credentials or the correct SSH user are still needed before remote setup work can proceed.
- SSH access to Bot 2 works with `root@5.161.102.170` and the provided password. The live service is `hermes-gateway.service`, running as user `hermes`, with `ExecStart=/home/hermes/.local/bin/hermes gateway run` and working directory `/home/hermes`. Likely code/config locations are under `/home/hermes/.hermes/hermes-agent`, with config at `/home/hermes/.hermes/config.yaml` and environment file `/home/hermes/.hermes/.env`.
- Installed Tailscale on Bot 2 (`5.161.102.170`) successfully and enabled `tailscaled`. It was then authenticated into Lionel's tailnet successfully. Verified Tailscale IP is `100.95.180.106` (`ubuntu-4gb-ash-2`), and the current OpenClaw host can ping it over Tailscale. This clears the network prerequisite for converting the box from Discord-bot mode toward Hermes worker-gateway mode.
- Set up a parallel Hermes worker gateway on Bot 2 without touching the live Discord bot. Created Hermes profiles `marketing`, `development`, and `operations`, copied the existing auth store into them, replaced their cloned kawaii display personality with concise behavior, and installed role-specific `SOUL.md` files.
- Deployed and started a new systemd service `hermes-worker-gateway.service` on Bot 2. It runs `/home/hermes/worker-gateway/worker_gateway.py` as user `hermes`, binds to the box's Tailscale IP, and exposes `GET /v1/health`, `POST /v1/tasks`, `GET /v1/tasks/{id}`, and `POST /v1/tasks/{id}/cancel`.
- Verified the new worker gateway end-to-end over Tailscale from this OpenClaw host: health returned all three workers ready, task creation worked with bearer auth, polling worked, and a sample marketing task executed successfully through the `marketing` Hermes profile and returned structured `needs_review` output.
- Wrote `HERMES-HYBRID-SETUP-RUNBOOK.md` to document the whole hybrid setup process end-to-end: architecture, what was changed on Bot 2, live services/paths, API surface, verification steps, current limitations, and the remaining Command-side integration work. This doc is intended for the sanitized git export branch.
- Added a first Command-side Hermes bridge in the OpenClaw workspace: `skills/hermes-dispatch/SKILL.md` plus `skills/hermes-dispatch/scripts/hermes_gateway_client.py`. The skill now loads in OpenClaw and the client can reach the live Bot 2 gateway over Tailscale via `health`, confirming all three workers are ready from the Command host side. Full task submission is still blocked on copying the bearer token into a local untracked `.env.hermes-gateway` file because remote Bot 2 SSH auth was not available in this session.
# 2026-04-16

- `memory-core` semantic memory is now working with OpenAI embeddings on this OpenClaw instance. The active setup is `plugins.slots.memory = memory-core` plus `agents.defaults.memorySearch.provider = openai` and `model = text-embedding-3-small`, with the OpenAI key resolved from `${OPENAI_API_KEY}`. After billing was added, deep status/index succeeded and semantic searches returned relevant hits from recent memory files.
- Important memory model note for future explanations: in `memory-core`, the Markdown memory files are the source of truth and embeddings are only a semantic index over chunks of those files, not a separate store of the real memory.
- Lionel approved the high-level multi-agent direction: keep Command/OpenClaw as the orchestrator with TaskFlow and memory on one VPS, and likely consolidate Hermes 1/2/3 onto a separate worker VPS behind a single Hermes gateway. Recommended long-term link is Tailscale-only HTTP, with TaskFlow remaining canonical on Command and Hermes treated as mostly stateless workers.
- Draft architecture docs were created in the workspace: `HERMES-ORCHESTRATION-V2.md`, `HERMES-GATEWAY-API-V1.md`, `specs/hermes-gateway-v1.openapi.yaml`, and `specs/hermes-task.v1.schema.json`.
- A sanitized export of the setup was pushed to GitHub repo `musical-basics/lionel-openclaw-1` on branch `setup-export`. Raw local history was intentionally not pushed because the workspace repo contains private memory/history and secrets/runtime state that should not be published as-is.
- Hermes connection test status: SSH connectivity to the candidate Hermes server succeeded, but the provided root login failed at authentication. Durable takeaway is that network reachability is fine, but valid credentials or the correct SSH user are still needed before remote setup work can proceed.
- SSH access to Bot 2 works with `root@5.161.102.170` and the provided password. The live service is `hermes-gateway.service`, running as user `hermes`, with `ExecStart=/home/hermes/.local/bin/hermes gateway run` and working directory `/home/hermes`. Likely code/config locations are under `/home/hermes/.hermes/hermes-agent`, with config at `/home/hermes/.hermes/config.yaml` and environment file `/home/hermes/.hermes/.env`.
- Installed Tailscale on Bot 2 (`5.161.102.170`) successfully and enabled `tailscaled`. It was then authenticated into Lionel's tailnet successfully. Verified Tailscale IP is `100.95.180.106` (`ubuntu-4gb-ash-2`), and the current OpenClaw host can ping it over Tailscale. This clears the network prerequisite for converting the box from Discord-bot mode toward Hermes worker-gateway mode.
- Set up a parallel Hermes worker gateway on Bot 2 without touching the live Discord bot. Created Hermes profiles `marketing`, `development`, and `operations`, copied the existing auth store into them, replaced their cloned kawaii display personality with concise behavior, and installed role-specific `SOUL.md` files.
- Deployed and started a new systemd service `hermes-worker-gateway.service` on Bot 2. It runs `/home/hermes/worker-gateway/worker_gateway.py` as user `hermes`, binds to the box's Tailscale IP, and exposes `GET /v1/health`, `POST /v1/tasks`, `GET /v1/tasks/{id}`, and `POST /v1/tasks/{id}/cancel`.
- Verified the new worker gateway end-to-end over Tailscale from this OpenClaw host: health returned all three workers ready, task creation worked with bearer auth, polling worked, and a sample marketing task executed successfully through the `marketing` Hermes profile and returned structured `needs_review` output.
- Wrote `HERMES-HYBRID-SETUP-RUNBOOK.md` to document the whole hybrid setup process end-to-end: architecture, what was changed on Bot 2, live services/paths, API surface, verification steps, current limitations, and the remaining Command-side integration work. This doc is intended for the sanitized git export branch.
- Added a first Command-side Hermes bridge in the OpenClaw workspace: `skills/hermes-dispatch/SKILL.md` plus `skills/hermes-dispatch/scripts/hermes_gateway_client.py`. The skill now loads in OpenClaw and the client can reach the live Bot 2 gateway over Tailscale via `health`, confirming all three workers are ready from the Command host side. Full task submission is still blocked on copying the bearer token into a local untracked `.env.hermes-gateway` file because remote Bot 2 SSH auth was not available in this session.
- Important user-facing mental model for future explanations: this setup is **3 logical Hermes workers behind 1 gateway service on 1 VPS**, not three separate gateway services. The workers are `marketing`, `development`, and `operations`. The old Discord Hermes bot is still running separately on the same box and is not part of the new worker-gateway model.
- The sanitized `setup-export` branch was updated again to include both the new runbook and the initial Command-side bridge artifacts. Current bridge state is partial: tailnet health checks work from Command to Bot 2, but real task submission still needs the bearer token on the Command box. Useful operational detail: `/v1/health` is reachable without auth on the tailnet, while task endpoints still require bearer auth.
- Lionel then chose the simpler same-VPS direction for the worker trio on the Command box itself, while explicitly keeping the remote Bot 2 worker stack untouched for separate management.
- On this VPS, three local isolated OpenClaw agents were created for the same worker roles: `marketing`, `development`, and `operations`. Their workspaces are `/home/openclaw/.openclaw/workspace-marketing`, `/home/openclaw/.openclaw/workspace-development`, and `/home/openclaw/.openclaw/workspace-operations`, with matching agent state under `/home/openclaw/.openclaw/agents/<id>/agent`.
- Important architecture nuance for future explanations: the local same-VPS version is not the separate Hermes CLI runtime. It is a Hermes-equivalent 3-worker split implemented natively as isolated OpenClaw agents on the same host, which is simpler and better for fast local iteration.
- Each local worker was given role-specific `SOUL.md`, `AGENTS.md`, and related workspace files so they behave like already-initialized specialists instead of first-run assistants.
- Verified all three local workers respond through the real OpenClaw path with `openclaw agent --agent <worker> --message ... --json`, and added a reusable local bridge skill/client: `skills/hermes-local-dispatch/SKILL.md` and `skills/hermes-local-dispatch/scripts/hermes_local_client.py`.
- Wrote `HERMES-SAME-VPS-ARCHITECTURE.md` as the canonical top-level doc for this local-worker architecture. Current recommended rule is: use local same-VPS workers first for work that can stay on this box; keep the remote Bot 2 stack as an optional separate worker environment.
# 2026-04-16

- `memory-core` semantic memory is now working with OpenAI embeddings on this OpenClaw instance. The active setup is `plugins.slots.memory = memory-core` plus `agents.defaults.memorySearch.provider = openai` and `model = text-embedding-3-small`, with the OpenAI key resolved from `${OPENAI_API_KEY}`. After billing was added, deep status/index succeeded and semantic searches returned relevant hits from recent memory files.
- Important memory model note for future explanations: in `memory-core`, the Markdown memory files are the source of truth and embeddings are only a semantic index over chunks of those files, not a separate store of the real memory.
- Lionel approved the high-level multi-agent direction: keep Command/OpenClaw as the orchestrator with TaskFlow and memory on one VPS, and likely consolidate Hermes 1/2/3 onto a separate worker VPS behind a single Hermes gateway. Recommended long-term link is Tailscale-only HTTP, with TaskFlow remaining canonical on Command and Hermes treated as mostly stateless workers.
- Draft architecture docs were created in the workspace: `HERMES-ORCHESTRATION-V2.md`, `HERMES-GATEWAY-API-V1.md`, `specs/hermes-gateway-v1.openapi.yaml`, and `specs/hermes-task.v1.schema.json`.
- A sanitized export of the setup was pushed to GitHub repo `musical-basics/lionel-openclaw-1` on branch `setup-export`. Raw local history was intentionally not pushed because the workspace repo contains private memory/history and secrets/runtime state that should not be published as-is.
- Hermes connection test status: SSH connectivity to the candidate Hermes server succeeded, but the provided root login failed at authentication. Durable takeaway is that network reachability is fine, but valid credentials or the correct SSH user are still needed before remote setup work can proceed.
- SSH access to Bot 2 works with `root@5.161.102.170` and the provided password. The live service is `hermes-gateway.service`, running as user `hermes`, with `ExecStart=/home/hermes/.local/bin/hermes gateway run` and working directory `/home/hermes`. Likely code/config locations are under `/home/hermes/.hermes/hermes-agent`, with config at `/home/hermes/.hermes/config.yaml` and environment file `/home/hermes/.hermes/.env`.
- Installed Tailscale on Bot 2 (`5.161.102.170`) successfully and enabled `tailscaled`. It was then authenticated into Lionel's tailnet successfully. Verified Tailscale IP is `100.95.180.106` (`ubuntu-4gb-ash-2`), and the current OpenClaw host can ping it over Tailscale. This clears the network prerequisite for converting the box from Discord-bot mode toward Hermes worker-gateway mode.
- Set up a parallel Hermes worker gateway on Bot 2 without touching the live Discord bot. Created Hermes profiles `marketing`, `development`, and `operations`, copied the existing auth store into them, replaced their cloned kawaii display personality with concise behavior, and installed role-specific `SOUL.md` files.
- Deployed and started a new systemd service `hermes-worker-gateway.service` on Bot 2. It runs `/home/hermes/worker-gateway/worker_gateway.py` as user `hermes`, binds to the box's Tailscale IP, and exposes `GET /v1/health`, `POST /v1/tasks`, `GET /v1/tasks/{id}`, and `POST /v1/tasks/{id}/cancel`.
- Verified the new worker gateway end-to-end over Tailscale from this OpenClaw host: health returned all three workers ready, task creation worked with bearer auth, polling worked, and a sample marketing task executed successfully through the `marketing` Hermes profile and returned structured `needs_review` output.
- Wrote `HERMES-HYBRID-SETUP-RUNBOOK.md` to document the whole hybrid setup process end-to-end: architecture, what was changed on Bot 2, live services/paths, API surface, verification steps, current limitations, and the remaining Command-side integration work. This doc is intended for the sanitized git export branch.
- Added a first Command-side Hermes bridge in the OpenClaw workspace: `skills/hermes-dispatch/SKILL.md` plus `skills/hermes-dispatch/scripts/hermes_gateway_client.py`. The skill now loads in OpenClaw and the client can reach the live Bot 2 gateway over Tailscale via `health`, confirming all three workers are ready from the Command host side. Full task submission is still blocked on copying the bearer token into a local untracked `.env.hermes-gateway` file because remote Bot 2 SSH auth was not available in this session.
# 2026-04-16

- `memory-core` semantic memory is now working with OpenAI embeddings on this OpenClaw instance. The active setup is `plugins.slots.memory = memory-core` plus `agents.defaults.memorySearch.provider = openai` and `model = text-embedding-3-small`, with the OpenAI key resolved from `${OPENAI_API_KEY}`. After billing was added, deep status/index succeeded and semantic searches returned relevant hits from recent memory files.
- Important memory model note for future explanations: in `memory-core`, the Markdown memory files are the source of truth and embeddings are only a semantic index over chunks of those files, not a separate store of the real memory.
- Lionel approved the high-level multi-agent direction: keep Command/OpenClaw as the orchestrator with TaskFlow and memory on one VPS, and likely consolidate Hermes 1/2/3 onto a separate worker VPS behind a single Hermes gateway. Recommended long-term link is Tailscale-only HTTP, with TaskFlow remaining canonical on Command and Hermes treated as mostly stateless workers.
- Draft architecture docs were created in the workspace: `HERMES-ORCHESTRATION-V2.md`, `HERMES-GATEWAY-API-V1.md`, `specs/hermes-gateway-v1.openapi.yaml`, and `specs/hermes-task.v1.schema.json`.
- A sanitized export of the setup was pushed to GitHub repo `musical-basics/lionel-openclaw-1` on branch `setup-export`. Raw local history was intentionally not pushed because the workspace repo contains private memory/history and secrets/runtime state that should not be published as-is.
- Hermes connection test status: SSH connectivity to the candidate Hermes server succeeded, but the provided root login failed at authentication. Durable takeaway is that network reachability is fine, but valid credentials or the correct SSH user are still needed before remote setup work can proceed.
- SSH access to Bot 2 works with `root@5.161.102.170` and the provided password. The live service is `hermes-gateway.service`, running as user `hermes`, with `ExecStart=/home/hermes/.local/bin/hermes gateway run` and working directory `/home/hermes`. Likely code/config locations are under `/home/hermes/.hermes/hermes-agent`, with config at `/home/hermes/.hermes/config.yaml` and environment file `/home/hermes/.hermes/.env`.
- Installed Tailscale on Bot 2 (`5.161.102.170`) successfully and enabled `tailscaled`. It was then authenticated into Lionel's tailnet successfully. Verified Tailscale IP is `100.95.180.106` (`ubuntu-4gb-ash-2`), and the current OpenClaw host can ping it over Tailscale. This clears the network prerequisite for converting the box from Discord-bot mode toward Hermes worker-gateway mode.
- Set up a parallel Hermes worker gateway on Bot 2 without touching the live Discord bot. Created Hermes profiles `marketing`, `development`, and `operations`, copied the existing auth store into them, replaced their cloned kawaii display personality with concise behavior, and installed role-specific `SOUL.md` files.
- Deployed and started a new systemd service `hermes-worker-gateway.service` on Bot 2. It runs `/home/hermes/worker-gateway/worker_gateway.py` as user `hermes`, binds to the box's Tailscale IP, and exposes `GET /v1/health`, `POST /v1/tasks`, `GET /v1/tasks/{id}`, and `POST /v1/tasks/{id}/cancel`.
- Verified the new worker gateway end-to-end over Tailscale from this OpenClaw host: health returned all three workers ready, task creation worked with bearer auth, polling worked, and a sample marketing task executed successfully through the `marketing` Hermes profile and returned structured `needs_review` output.
- Wrote `HERMES-HYBRID-SETUP-RUNBOOK.md` to document the whole hybrid setup process end-to-end: architecture, what was changed on Bot 2, live services/paths, API surface, verification steps, current limitations, and the remaining Command-side integration work. This doc is intended for the sanitized git export branch.
- Added a first Command-side Hermes bridge in the OpenClaw workspace: `skills/hermes-dispatch/SKILL.md` plus `skills/hermes-dispatch/scripts/hermes_gateway_client.py`. The skill now loads in OpenClaw and the client can reach the live Bot 2 gateway over Tailscale via `health`, confirming all three workers are ready from the Command host side. Full task submission is still blocked on copying the bearer token into a local untracked `.env.hermes-gateway` file because remote Bot 2 SSH auth was not available in this session.
- Important user-facing mental model for future explanations: this setup is **3 logical Hermes workers behind 1 gateway service on 1 VPS**, not three separate gateway services. The workers are `marketing`, `development`, and `operations`. The old Discord Hermes bot is still running separately on the same box and is not part of the new worker-gateway model.
- The sanitized `setup-export` branch was updated again to include both the new runbook and the initial Command-side bridge artifacts. Current bridge state is partial: tailnet health checks work from Command to Bot 2, but real task submission still needs the bearer token on the Command box. Useful operational detail: `/v1/health` is reachable without auth on the tailnet, while task endpoints still require bearer auth.
- Lionel then chose the simpler same-VPS direction for the worker trio on the Command box itself, while explicitly keeping the remote Bot 2 worker stack untouched for separate management.
- On this VPS, three local isolated OpenClaw agents were created for the same worker roles: `marketing`, `development`, and `operations`. Their workspaces are `/home/openclaw/.openclaw/workspace-marketing`, `/home/openclaw/.openclaw/workspace-development`, and `/home/openclaw/.openclaw/workspace-operations`, with matching agent state under `/home/openclaw/.openclaw/agents/<id>/agent`.
- Important architecture nuance for future explanations: the local same-VPS version is not the separate Hermes CLI runtime. It is a Hermes-equivalent 3-worker split implemented natively as isolated OpenClaw agents on the same host, which is simpler and better for fast local iteration.
- Each local worker was given role-specific `SOUL.md`, `AGENTS.md`, and related workspace files so they behave like already-initialized specialists instead of first-run assistants.
- Verified all three local workers respond through the real OpenClaw path with `openclaw agent --agent <worker> --message ... --json`, and added a reusable local bridge skill/client: `skills/hermes-local-dispatch/SKILL.md` and `skills/hermes-local-dispatch/scripts/hermes_local_client.py`.
- Wrote `HERMES-SAME-VPS-ARCHITECTURE.md` as the canonical top-level doc for this local-worker architecture. Current recommended rule is: use local same-VPS workers first for work that can stay on this box; keep the remote Bot 2 stack as an optional separate worker environment.
- During follow-on integration, the clean TaskFlow path for the local same-VPS workers was identified as OpenClaw's built-in `webhooks` plugin bound to this owner session, with a local route that can create/manage TaskFlows around worker dispatch. Important limitation discovered: that bridge only loads on gateway startup, so enabling it requires a local gateway restart on this VPS. Remote Bot 2 remains unaffected by that plan.
- Lionel approved the right auto-dispatch shape as inline brief synthesis in Command plus deterministic validation, not a separate extra generator pass.
- The local same-VPS dispatch client now has a real `prepare` step in `skills/hermes-local-dispatch/scripts/hermes_local_client.py`. Durable intent: synthesize the worker brief inline from normal chat, run `prepare` to normalize/validate/render the resolved brief and prompt, then run the worker.
- Added `skills/hermes-local-dispatch/references/auto-dispatch-playbook.md` to document worker selection, brief synthesis rules, guardrails, and the `prepare` then `run` flow.
- Verified the new local auto-dispatch prepare flow works end-to-end: `prepare` resolves role defaults and normalized brief fields correctly, and `run` still succeeds against a real local worker. Local commit: `9365f0f` (`Add local auto-dispatch prepare flow`).
- Important user-facing clarification for future explanations: the current local worker trio is ready for delegation now, but the 3 local workers are OpenClaw-native isolated agents, not Claude Code-backed agents. Claude Code can still be invoked separately for heavier coding work when needed.

- Claude Code access is now working for the local same-VPS `development` worker only. Durable implementation path: use `/home/openclaw/.openclaw/workspace/scripts/claude-development.sh`, which runs Claude with a writable custom HOME rooted at `/home/openclaw/.openclaw/workspace/.openclaw/claude-development-home`.
- Important nuance for future debugging: inside this OpenClaw sandbox, the plain default `claude` path can still fail with `401` because this environment cannot reliably persist refreshed auth state to `/home/openclaw/.claude`. The dev-only launcher works around that by keeping Claude's writable state in the workspace-local custom home.
- Verified the dev-only Claude path three ways: `auth status` reported logged in, `claude-development.sh -p "Reply with exactly: OK"` returned `OK`, and the `development` worker itself successfully invoked that launcher and returned `OK`.
- Preserve this split unless Lionel says otherwise: `development` gets Claude Code access, while local `marketing` and `operations` remain normal OpenClaw workers without Claude tooling.
- Workspace commit for this change: `0947e01` (`Add development-only Claude launcher`).
