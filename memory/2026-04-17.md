# 2026-04-17

- Lionel provided a DreamPlay Shopify orders export and asked that it become the local canonical snapshot instead of relying on live Shopify access for now.
- Imported the raw export to `data/dreamplay/orders/imports/2026-04-16-shopify-export.csv` and created `data/dreamplay/orders/2026-04-16-orders-normalized.csv` plus `data/dreamplay/orders/2026-04-16-orders-summary.json` for assistant use.
- Snapshot summary: 56 orders, total `Total` paid amount = `$28118.37`, with 45 full payments, 5 half reservations, and 6 waitlist reservations.
- Durable DreamPlay business rule: use `Total` as actual customer paid, use `Lineitem name` to classify reservation vs full payment, and do not treat older orders as the current price sheet because prices were increased.
- Lionel then provided a canonical DreamPlay business-agent brief, now saved as `DREAMPLAY-TIER1-CONTEXT.md`.
- Key Tier 1 context captured: DreamPlay builds narrow-key digital pianos for smaller hands; DreamPlay 1 is $999 base / $1,899 Pro anchor pricing; there are 55 preorders and about $18K of preorder revenue reserved for prototype work, not operations; zero customers cancelled after the delivery delay notice.
- Important Tier 1 operating rules captured: customer portal and preorder updates are top priorities; refunds, pricing changes, Stripe issues, public delivery statements, customer complaints, and manufacturing decisions must be escalated to Lionel.
- Lionel clarified the role mapping explicitly: the main assistant is the DreamPlay Tier 1 bot, and the delegated worker is the Tier 2 bot.
- Current DreamPlay product task: customer portal for preorder buyers. Portal must let logged-in customers see their preorder status, amount paid, current selected choice, and three wired actions: Keep, Upgrade, Cancel.
- Tier 1 spec for this was created at `specs/dreamplay-customer-portal-v1-tier1-spec.md`. Current Tier 1 recommendation is that Upgrade and Cancel should be request/managed flows in V1 rather than fully automatic pricing/refund execution unless Lionel explicitly approves that.
- Repo analysis pass completed for `dreamplay-website-3`. Saved permanent high-level codebase context at `dreamplay-website-3/docs/REPO-CONTEXT.md`, including the key finding that the repo already has Supabase auth and a buyer decision portal, but does not yet have real Shopify Admin API order sync or true order-status integration.
- Turned the vague phrase "put these orders into the system" into a concrete Tier 1 implementation brief at `specs/dreamplay-orders-into-system-v1-tier1-spec.md`. Current Tier 1 recommendation: for V1, treat the imported local order snapshot as the canonical input, load it into the website's internal Supabase-backed order layer, and drive `/my-reservation` from that instead of requiring live Shopify access first.
# 2026-04-17

- Lionel provided a DreamPlay Shopify orders export and asked that it become the local canonical snapshot instead of relying on live Shopify access for now.
- Imported the raw export to `data/dreamplay/orders/imports/2026-04-16-shopify-export.csv` and created `data/dreamplay/orders/2026-04-16-orders-normalized.csv` plus `data/dreamplay/orders/2026-04-16-orders-summary.json` for assistant use.
- Snapshot summary: 56 orders, total `Total` paid amount = `$28118.37`, with 45 full payments, 5 half reservations, and 6 waitlist reservations.
- Durable DreamPlay business rule: use `Total` as actual customer paid, use `Lineitem name` to classify reservation vs full payment, and do not treat older orders as the current price sheet because prices were increased.
- Lionel then provided a canonical DreamPlay business-agent brief, now saved as `DREAMPLAY-TIER1-CONTEXT.md`.
- Key Tier 1 context captured: DreamPlay builds narrow-key digital pianos for smaller hands; DreamPlay 1 is $999 base / $1,899 Pro anchor pricing; there are 55 preorders and about $18K of preorder revenue reserved for prototype work, not operations; zero customers cancelled after the delivery delay notice.
- Important Tier 1 operating rules captured: customer portal and preorder updates are top priorities; refunds, pricing changes, Stripe issues, public delivery statements, customer complaints, and manufacturing decisions must be escalated to Lionel.
- Lionel clarified the role mapping explicitly: the main assistant is the DreamPlay Tier 1 bot, and the delegated worker is the Tier 2 bot.
- Current DreamPlay product task: customer portal for preorder buyers. Portal must let logged-in customers see their preorder status, amount paid, current selected choice, and three wired actions: Keep, Upgrade, Cancel.
- Tier 1 spec for this was created at `specs/dreamplay-customer-portal-v1-tier1-spec.md`. Current Tier 1 recommendation is that Upgrade and Cancel should be request/managed flows in V1 rather than fully automatic pricing/refund execution unless Lionel explicitly approves that.
- Repo analysis pass completed for `dreamplay-website-3`. Saved permanent high-level codebase context at `dreamplay-website-3/docs/REPO-CONTEXT.md`, including the key finding that the repo already has Supabase auth and a buyer decision portal, but does not yet have real Shopify Admin API order sync or true order-status integration.
- Turned the vague phrase "put these orders into the system" into a concrete Tier 1 implementation brief at `specs/dreamplay-orders-into-system-v1-tier1-spec.md`. Current Tier 1 recommendation: for V1, treat the imported local order snapshot as the canonical input, load it into the website's internal Supabase-backed order layer, and drive `/my-reservation` from that instead of requiring live Shopify access first.
- Lionel asked how memory works; current durable-memory framing is text files as source of truth with embeddings used only as a semantic recall layer.
- Lionel showed interest in switching memory embeddings from `text-embedding-3-small` to `text-embedding-3-large`; if adopted later, the memory index would need to be re-embedded/rebuilt.
- Saved the approved Tier 2 Claude Code worker brief at `specs/dreamplay-orders-into-system-v1-tier2-claude-prompt.md`.
- Current auth-gating finding for `dreamplay-website-3`: successful login alone is not enough for `/my-reservation`; the email must also exist in the `buyer_emails` table or the app redirects the user to `/vip`.
- Created a DreamPlay auth test user and verified the current flow locally: login succeeds, `/vip` loads, and `/my-reservation` redirects to `/vip` when the user is not in `buyer_emails`.
- Current blocker to fully testing `/my-reservation` with a fresh test account: the available Supabase key can create auth users but does not have permission to insert into `buyer_emails`.
- Lionel asked to try `lionel@musicalbasics.com` instead, since it is already allowlisted in `buyer_emails`.
- Verified locally with a created Supabase auth user for `lionel@musicalbasics.com`: login succeeds, `/vip` redirects to `/my-reservation`, and `/my-reservation` returns 200 and renders the reservation portal.
# 2026-04-17

- Lionel provided a DreamPlay Shopify orders export and asked that it become the local canonical snapshot instead of relying on live Shopify access for now.
- Imported the raw export to `data/dreamplay/orders/imports/2026-04-16-shopify-export.csv` and created `data/dreamplay/orders/2026-04-16-orders-normalized.csv` plus `data/dreamplay/orders/2026-04-16-orders-summary.json` for assistant use.
- Snapshot summary: 56 orders, total `Total` paid amount = `$28118.37`, with 45 full payments, 5 half reservations, and 6 waitlist reservations.
- Durable DreamPlay business rule: use `Total` as actual customer paid, use `Lineitem name` to classify reservation vs full payment, and do not treat older orders as the current price sheet because prices were increased.
- Lionel then provided a canonical DreamPlay business-agent brief, now saved as `DREAMPLAY-TIER1-CONTEXT.md`.
- Key Tier 1 context captured: DreamPlay builds narrow-key digital pianos for smaller hands; DreamPlay 1 is $999 base / $1,899 Pro anchor pricing; there are 55 preorders and about $18K of preorder revenue reserved for prototype work, not operations; zero customers cancelled after the delivery delay notice.
- Important Tier 1 operating rules captured: customer portal and preorder updates are top priorities; refunds, pricing changes, Stripe issues, public delivery statements, customer complaints, and manufacturing decisions must be escalated to Lionel.
- Lionel clarified the role mapping explicitly: the main assistant is the DreamPlay Tier 1 bot, and the delegated worker is the Tier 2 bot.
- Current DreamPlay product task: customer portal for preorder buyers. Portal must let logged-in customers see their preorder status, amount paid, current selected choice, and three wired actions: Keep, Upgrade, Cancel.
- Tier 1 spec for this was created at `specs/dreamplay-customer-portal-v1-tier1-spec.md`. Current Tier 1 recommendation is that Upgrade and Cancel should be request/managed flows in V1 rather than fully automatic pricing/refund execution unless Lionel explicitly approves that.
- Repo analysis pass completed for `dreamplay-website-3`. Saved permanent high-level codebase context at `dreamplay-website-3/docs/REPO-CONTEXT.md`, including the key finding that the repo already has Supabase auth and a buyer decision portal, but does not yet have real Shopify Admin API order sync or true order-status integration.
- Turned the vague phrase "put these orders into the system" into a concrete Tier 1 implementation brief at `specs/dreamplay-orders-into-system-v1-tier1-spec.md`. Current Tier 1 recommendation: for V1, treat the imported local order snapshot as the canonical input, load it into the website's internal Supabase-backed order layer, and drive `/my-reservation` from that instead of requiring live Shopify access first.
# 2026-04-17

- Lionel provided a DreamPlay Shopify orders export and asked that it become the local canonical snapshot instead of relying on live Shopify access for now.
- Imported the raw export to `data/dreamplay/orders/imports/2026-04-16-shopify-export.csv` and created `data/dreamplay/orders/2026-04-16-orders-normalized.csv` plus `data/dreamplay/orders/2026-04-16-orders-summary.json` for assistant use.
- Snapshot summary: 56 orders, total `Total` paid amount = `$28118.37`, with 45 full payments, 5 half reservations, and 6 waitlist reservations.
- Durable DreamPlay business rule: use `Total` as actual customer paid, use `Lineitem name` to classify reservation vs full payment, and do not treat older orders as the current price sheet because prices were increased.
- Lionel then provided a canonical DreamPlay business-agent brief, now saved as `DREAMPLAY-TIER1-CONTEXT.md`.
- Key Tier 1 context captured: DreamPlay builds narrow-key digital pianos for smaller hands; DreamPlay 1 is $999 base / $1,899 Pro anchor pricing; there are 55 preorders and about $18K of preorder revenue reserved for prototype work, not operations; zero customers cancelled after the delivery delay notice.
- Important Tier 1 operating rules captured: customer portal and preorder updates are top priorities; refunds, pricing changes, Stripe issues, public delivery statements, customer complaints, and manufacturing decisions must be escalated to Lionel.
- Lionel clarified the role mapping explicitly: the main assistant is the DreamPlay Tier 1 bot, and the delegated worker is the Tier 2 bot.
- Current DreamPlay product task: customer portal for preorder buyers. Portal must let logged-in customers see their preorder status, amount paid, current selected choice, and three wired actions: Keep, Upgrade, Cancel.
- Tier 1 spec for this was created at `specs/dreamplay-customer-portal-v1-tier1-spec.md`. Current Tier 1 recommendation is that Upgrade and Cancel should be request/managed flows in V1 rather than fully automatic pricing/refund execution unless Lionel explicitly approves that.
- Repo analysis pass completed for `dreamplay-website-3`. Saved permanent high-level codebase context at `dreamplay-website-3/docs/REPO-CONTEXT.md`, including the key finding that the repo already has Supabase auth and a buyer decision portal, but does not yet have real Shopify Admin API order sync or true order-status integration.
- Turned the vague phrase "put these orders into the system" into a concrete Tier 1 implementation brief at `specs/dreamplay-orders-into-system-v1-tier1-spec.md`. Current Tier 1 recommendation: for V1, treat the imported local order snapshot as the canonical input, load it into the website's internal Supabase-backed order layer, and drive `/my-reservation` from that instead of requiring live Shopify access first.
- Lionel asked how memory works; current durable-memory framing is text files as source of truth with embeddings used only as a semantic recall layer.
- Lionel showed interest in switching memory embeddings from `text-embedding-3-small` to `text-embedding-3-large`; if adopted later, the memory index would need to be re-embedded/rebuilt.
- Saved the approved Tier 2 Claude Code worker brief at `specs/dreamplay-orders-into-system-v1-tier2-claude-prompt.md`.
- Current auth-gating finding for `dreamplay-website-3`: successful login alone is not enough for `/my-reservation`; the email must also exist in the `buyer_emails` table or the app redirects the user to `/vip`.
- Created a DreamPlay auth test user and verified the current flow locally: login succeeds, `/vip` loads, and `/my-reservation` redirects to `/vip` when the user is not in `buyer_emails`.
- Current blocker to fully testing `/my-reservation` with a fresh test account: the available Supabase key can create auth users but does not have permission to insert into `buyer_emails`.
- Lionel asked to try `lionel@musicalbasics.com` instead, since it is already allowlisted in `buyer_emails`.
- Verified locally with a created Supabase auth user for `lionel@musicalbasics.com`: login succeeds, `/vip` redirects to `/my-reservation`, and `/my-reservation` returns 200 and renders the reservation portal.
- Lionel then hit a Vercel deploy failure for `dreamplay-website-3`; likely cause identified was missing or misnamed Supabase env vars in Vercel rather than a deeper code bug.
- The key env names the repo expects are `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, and `SUPABASE_SERVICE_ROLE_KEY` specifically, not `SUPABASE_SERVICE_KEY`.
- Build-time failure source found: `src/app/api/chat-session/route.ts` and `src/actions/admin-actions.ts` both create Supabase clients at module load, so `next build` can crash during route/page data collection when those env vars are absent.
- If the Vercel env issue persists, the next code-hardening fix would be to lazy-initialize those Supabase clients so missing envs fail later and more locally.
# 2026-04-17

- Lionel provided a DreamPlay Shopify orders export and asked that it become the local canonical snapshot instead of relying on live Shopify access for now.
- Imported the raw export to `data/dreamplay/orders/imports/2026-04-16-shopify-export.csv` and created `data/dreamplay/orders/2026-04-16-orders-normalized.csv` plus `data/dreamplay/orders/2026-04-16-orders-summary.json` for assistant use.
- Snapshot summary: 56 orders, total `Total` paid amount = `$28118.37`, with 45 full payments, 5 half reservations, and 6 waitlist reservations.
- Durable DreamPlay business rule: use `Total` as actual customer paid, use `Lineitem name` to classify reservation vs full payment, and do not treat older orders as the current price sheet because prices were increased.
- Lionel then provided a canonical DreamPlay business-agent brief, now saved as `DREAMPLAY-TIER1-CONTEXT.md`.
- Key Tier 1 context captured: DreamPlay builds narrow-key digital pianos for smaller hands; DreamPlay 1 is $999 base / $1,899 Pro anchor pricing; there are 55 preorders and about $18K of preorder revenue reserved for prototype work, not operations; zero customers cancelled after the delivery delay notice.
- Important Tier 1 operating rules captured: customer portal and preorder updates are top priorities; refunds, pricing changes, Stripe issues, public delivery statements, customer complaints, and manufacturing decisions must be escalated to Lionel.
- Lionel clarified the role mapping explicitly: the main assistant is the DreamPlay Tier 1 bot, and the delegated worker is the Tier 2 bot.
- Current DreamPlay product task: customer portal for preorder buyers. Portal must let logged-in customers see their preorder status, amount paid, current selected choice, and three wired actions: Keep, Upgrade, Cancel.
- Tier 1 spec for this was created at `specs/dreamplay-customer-portal-v1-tier1-spec.md`. Current Tier 1 recommendation is that Upgrade and Cancel should be request/managed flows in V1 rather than fully automatic pricing/refund execution unless Lionel explicitly approves that.
- Repo analysis pass completed for `dreamplay-website-3`. Saved permanent high-level codebase context at `dreamplay-website-3/docs/REPO-CONTEXT.md`, including the key finding that the repo already has Supabase auth and a buyer decision portal, but does not yet have real Shopify Admin API order sync or true order-status integration.
- Turned the vague phrase "put these orders into the system" into a concrete Tier 1 implementation brief at `specs/dreamplay-orders-into-system-v1-tier1-spec.md`. Current Tier 1 recommendation: for V1, treat the imported local order snapshot as the canonical input, load it into the website's internal Supabase-backed order layer, and drive `/my-reservation` from that instead of requiring live Shopify access first.
# 2026-04-17

- Lionel provided a DreamPlay Shopify orders export and asked that it become the local canonical snapshot instead of relying on live Shopify access for now.
- Imported the raw export to `data/dreamplay/orders/imports/2026-04-16-shopify-export.csv` and created `data/dreamplay/orders/2026-04-16-orders-normalized.csv` plus `data/dreamplay/orders/2026-04-16-orders-summary.json` for assistant use.
- Snapshot summary: 56 orders, total `Total` paid amount = `$28118.37`, with 45 full payments, 5 half reservations, and 6 waitlist reservations.
- Durable DreamPlay business rule: use `Total` as actual customer paid, use `Lineitem name` to classify reservation vs full payment, and do not treat older orders as the current price sheet because prices were increased.
- Lionel then provided a canonical DreamPlay business-agent brief, now saved as `DREAMPLAY-TIER1-CONTEXT.md`.
- Key Tier 1 context captured: DreamPlay builds narrow-key digital pianos for smaller hands; DreamPlay 1 is $999 base / $1,899 Pro anchor pricing; there are 55 preorders and about $18K of preorder revenue reserved for prototype work, not operations; zero customers cancelled after the delivery delay notice.
- Important Tier 1 operating rules captured: customer portal and preorder updates are top priorities; refunds, pricing changes, Stripe issues, public delivery statements, customer complaints, and manufacturing decisions must be escalated to Lionel.
- Lionel clarified the role mapping explicitly: the main assistant is the DreamPlay Tier 1 bot, and the delegated worker is the Tier 2 bot.
- Current DreamPlay product task: customer portal for preorder buyers. Portal must let logged-in customers see their preorder status, amount paid, current selected choice, and three wired actions: Keep, Upgrade, Cancel.
- Tier 1 spec for this was created at `specs/dreamplay-customer-portal-v1-tier1-spec.md`. Current Tier 1 recommendation is that Upgrade and Cancel should be request/managed flows in V1 rather than fully automatic pricing/refund execution unless Lionel explicitly approves that.
- Repo analysis pass completed for `dreamplay-website-3`. Saved permanent high-level codebase context at `dreamplay-website-3/docs/REPO-CONTEXT.md`, including the key finding that the repo already has Supabase auth and a buyer decision portal, but does not yet have real Shopify Admin API order sync or true order-status integration.
- Turned the vague phrase "put these orders into the system" into a concrete Tier 1 implementation brief at `specs/dreamplay-orders-into-system-v1-tier1-spec.md`. Current Tier 1 recommendation: for V1, treat the imported local order snapshot as the canonical input, load it into the website's internal Supabase-backed order layer, and drive `/my-reservation` from that instead of requiring live Shopify access first.
- Lionel asked how memory works; current durable-memory framing is text files as source of truth with embeddings used only as a semantic recall layer.
- Lionel showed interest in switching memory embeddings from `text-embedding-3-small` to `text-embedding-3-large`; if adopted later, the memory index would need to be re-embedded/rebuilt.
- Saved the approved Tier 2 Claude Code worker brief at `specs/dreamplay-orders-into-system-v1-tier2-claude-prompt.md`.
- Current auth-gating finding for `dreamplay-website-3`: successful login alone is not enough for `/my-reservation`; the email must also exist in the `buyer_emails` table or the app redirects the user to `/vip`.
- Created a DreamPlay auth test user and verified the current flow locally: login succeeds, `/vip` loads, and `/my-reservation` redirects to `/vip` when the user is not in `buyer_emails`.
- Current blocker to fully testing `/my-reservation` with a fresh test account: the available Supabase key can create auth users but does not have permission to insert into `buyer_emails`.
- Lionel asked to try `lionel@musicalbasics.com` instead, since it is already allowlisted in `buyer_emails`.
- Verified locally with a created Supabase auth user for `lionel@musicalbasics.com`: login succeeds, `/vip` redirects to `/my-reservation`, and `/my-reservation` returns 200 and renders the reservation portal.
# 2026-04-17

- Lionel provided a DreamPlay Shopify orders export and asked that it become the local canonical snapshot instead of relying on live Shopify access for now.
- Imported the raw export to `data/dreamplay/orders/imports/2026-04-16-shopify-export.csv` and created `data/dreamplay/orders/2026-04-16-orders-normalized.csv` plus `data/dreamplay/orders/2026-04-16-orders-summary.json` for assistant use.
- Snapshot summary: 56 orders, total `Total` paid amount = `$28118.37`, with 45 full payments, 5 half reservations, and 6 waitlist reservations.
- Durable DreamPlay business rule: use `Total` as actual customer paid, use `Lineitem name` to classify reservation vs full payment, and do not treat older orders as the current price sheet because prices were increased.
- Lionel then provided a canonical DreamPlay business-agent brief, now saved as `DREAMPLAY-TIER1-CONTEXT.md`.
- Key Tier 1 context captured: DreamPlay builds narrow-key digital pianos for smaller hands; DreamPlay 1 is $999 base / $1,899 Pro anchor pricing; there are 55 preorders and about $18K of preorder revenue reserved for prototype work, not operations; zero customers cancelled after the delivery delay notice.
- Important Tier 1 operating rules captured: customer portal and preorder updates are top priorities; refunds, pricing changes, Stripe issues, public delivery statements, customer complaints, and manufacturing decisions must be escalated to Lionel.
- Lionel clarified the role mapping explicitly: the main assistant is the DreamPlay Tier 1 bot, and the delegated worker is the Tier 2 bot.
- Current DreamPlay product task: customer portal for preorder buyers. Portal must let logged-in customers see their preorder status, amount paid, current selected choice, and three wired actions: Keep, Upgrade, Cancel.
- Tier 1 spec for this was created at `specs/dreamplay-customer-portal-v1-tier1-spec.md`. Current Tier 1 recommendation is that Upgrade and Cancel should be request/managed flows in V1 rather than fully automatic pricing/refund execution unless Lionel explicitly approves that.
- Repo analysis pass completed for `dreamplay-website-3`. Saved permanent high-level codebase context at `dreamplay-website-3/docs/REPO-CONTEXT.md`, including the key finding that the repo already has Supabase auth and a buyer decision portal, but does not yet have real Shopify Admin API order sync or true order-status integration.
- Turned the vague phrase "put these orders into the system" into a concrete Tier 1 implementation brief at `specs/dreamplay-orders-into-system-v1-tier1-spec.md`. Current Tier 1 recommendation: for V1, treat the imported local order snapshot as the canonical input, load it into the website's internal Supabase-backed order layer, and drive `/my-reservation` from that instead of requiring live Shopify access first.
# 2026-04-17

- Lionel provided a DreamPlay Shopify orders export and asked that it become the local canonical snapshot instead of relying on live Shopify access for now.
- Imported the raw export to `data/dreamplay/orders/imports/2026-04-16-shopify-export.csv` and created `data/dreamplay/orders/2026-04-16-orders-normalized.csv` plus `data/dreamplay/orders/2026-04-16-orders-summary.json` for assistant use.
- Snapshot summary: 56 orders, total `Total` paid amount = `$28118.37`, with 45 full payments, 5 half reservations, and 6 waitlist reservations.
- Durable DreamPlay business rule: use `Total` as actual customer paid, use `Lineitem name` to classify reservation vs full payment, and do not treat older orders as the current price sheet because prices were increased.
- Lionel then provided a canonical DreamPlay business-agent brief, now saved as `DREAMPLAY-TIER1-CONTEXT.md`.
- Key Tier 1 context captured: DreamPlay builds narrow-key digital pianos for smaller hands; DreamPlay 1 is $999 base / $1,899 Pro anchor pricing; there are 55 preorders and about $18K of preorder revenue reserved for prototype work, not operations; zero customers cancelled after the delivery delay notice.
- Important Tier 1 operating rules captured: customer portal and preorder updates are top priorities; refunds, pricing changes, Stripe issues, public delivery statements, customer complaints, and manufacturing decisions must be escalated to Lionel.
- Lionel clarified the role mapping explicitly: the main assistant is the DreamPlay Tier 1 bot, and the delegated worker is the Tier 2 bot.
- Current DreamPlay product task: customer portal for preorder buyers. Portal must let logged-in customers see their preorder status, amount paid, current selected choice, and three wired actions: Keep, Upgrade, Cancel.
- Tier 1 spec for this was created at `specs/dreamplay-customer-portal-v1-tier1-spec.md`. Current Tier 1 recommendation is that Upgrade and Cancel should be request/managed flows in V1 rather than fully automatic pricing/refund execution unless Lionel explicitly approves that.
- Repo analysis pass completed for `dreamplay-website-3`. Saved permanent high-level codebase context at `dreamplay-website-3/docs/REPO-CONTEXT.md`, including the key finding that the repo already has Supabase auth and a buyer decision portal, but does not yet have real Shopify Admin API order sync or true order-status integration.
- Turned the vague phrase "put these orders into the system" into a concrete Tier 1 implementation brief at `specs/dreamplay-orders-into-system-v1-tier1-spec.md`. Current Tier 1 recommendation: for V1, treat the imported local order snapshot as the canonical input, load it into the website's internal Supabase-backed order layer, and drive `/my-reservation` from that instead of requiring live Shopify access first.
- Lionel asked how memory works; current durable-memory framing is text files as source of truth with embeddings used only as a semantic recall layer.
- Lionel showed interest in switching memory embeddings from `text-embedding-3-small` to `text-embedding-3-large`; if adopted later, the memory index would need to be re-embedded/rebuilt.
- Saved the approved Tier 2 Claude Code worker brief at `specs/dreamplay-orders-into-system-v1-tier2-claude-prompt.md`.
- Current auth-gating finding for `dreamplay-website-3`: successful login alone is not enough for `/my-reservation`; the email must also exist in the `buyer_emails` table or the app redirects the user to `/vip`.
- Created a DreamPlay auth test user and verified the current flow locally: login succeeds, `/vip` loads, and `/my-reservation` redirects to `/vip` when the user is not in `buyer_emails`.
- Current blocker to fully testing `/my-reservation` with a fresh test account: the available Supabase key can create auth users but does not have permission to insert into `buyer_emails`.
- Lionel asked to try `lionel@musicalbasics.com` instead, since it is already allowlisted in `buyer_emails`.
- Verified locally with a created Supabase auth user for `lionel@musicalbasics.com`: login succeeds, `/vip` redirects to `/my-reservation`, and `/my-reservation` returns 200 and renders the reservation portal.
- Lionel then hit a Vercel deploy failure for `dreamplay-website-3`; likely cause identified was missing or misnamed Supabase env vars in Vercel rather than a deeper code bug.
- The key env names the repo expects are `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, and `SUPABASE_SERVICE_ROLE_KEY` specifically, not `SUPABASE_SERVICE_KEY`.
- Build-time failure source found: `src/app/api/chat-session/route.ts` and `src/actions/admin-actions.ts` both create Supabase clients at module load, so `next build` can crash during route/page data collection when those env vars are absent.
- If the Vercel env issue persists, the next code-hardening fix would be to lazy-initialize those Supabase clients so missing envs fail later and more locally.
- Implemented the DreamPlay preorder order layer V1 in `dreamplay-website-3` and committed it as `4ee171e` with message `Implement preorder order layer v1`.
- The V1 implementation added `src/lib/preorders.ts`, `scripts/import-preorders.ts`, `scripts/verify-preorder-logic.ts`, preorder-related Supabase migrations under `supabase/migrations/`, and the implementation note `docs/preorder-order-layer-v1.md`, plus wiring updates in the reservation portal files.
- Important bug fix after the Claude pass: `saveReservationDecision` now writes the real `preorder_id` foreign-key column when available, while still falling back gracefully to `order_metadata` if the migration has not been applied yet.
- Verification status for the implementation: targeted lint passed after cleanup, `npx tsx scripts/verify-preorder-logic.ts` passed with `26 passed, 0 failed`, and a local Next build passed once the expected Supabase env vars were supplied in a gitignored `.env.local`.
- Lionel confirmed that the Vercel project already has the correct env variable names set. The working theory is now that the failed deployment was an env availability/timing issue on that deployment, so the next recommended step is a fresh redeploy before doing deeper code changes.
- Discord bot-mention troubleshooting note from `#general`: plain-text/name-style `@Openclaw 1` / `@Openclaw 2` references were not reliable enough for bot-to-bot ping confirmation. The observed reliable fix was to use raw Discord mentions directly, especially `<@1494663087373160580>` for Openclaw 1, `<@1494531954618536007>` for Openclaw 2, and `<@1494656974732656681>` for Openclaw 3. Once raw mentions were used in the same message, cross-bot visibility worked as expected in-channel.
# 2026-04-17

- Lionel provided a DreamPlay Shopify orders export and asked that it become the local canonical snapshot instead of relying on live Shopify access for now.
- Imported the raw export to `data/dreamplay/orders/imports/2026-04-16-shopify-export.csv` and created `data/dreamplay/orders/2026-04-16-orders-normalized.csv` plus `data/dreamplay/orders/2026-04-16-orders-summary.json` for assistant use.
- Snapshot summary: 56 orders, total `Total` paid amount = `$28118.37`, with 45 full payments, 5 half reservations, and 6 waitlist reservations.
- Durable DreamPlay business rule: use `Total` as actual customer paid, use `Lineitem name` to classify reservation vs full payment, and do not treat older orders as the current price sheet because prices were increased.
- Lionel then provided a canonical DreamPlay business-agent brief, now saved as `DREAMPLAY-TIER1-CONTEXT.md`.
- Key Tier 1 context captured: DreamPlay builds narrow-key digital pianos for smaller hands; DreamPlay 1 is $999 base / $1,899 Pro anchor pricing; there are 55 preorders and about $18K of preorder revenue reserved for prototype work, not operations; zero customers cancelled after the delivery delay notice.
- Important Tier 1 operating rules captured: customer portal and preorder updates are top priorities; refunds, pricing changes, Stripe issues, public delivery statements, customer complaints, and manufacturing decisions must be escalated to Lionel.
- Lionel clarified the role mapping explicitly: the main assistant is the DreamPlay Tier 1 bot, and the delegated worker is the Tier 2 bot.
- Current DreamPlay product task: customer portal for preorder buyers. Portal must let logged-in customers see their preorder status, amount paid, current selected choice, and three wired actions: Keep, Upgrade, Cancel.
- Tier 1 spec for this was created at `specs/dreamplay-customer-portal-v1-tier1-spec.md`. Current Tier 1 recommendation is that Upgrade and Cancel should be request/managed flows in V1 rather than fully automatic pricing/refund execution unless Lionel explicitly approves that.
- Repo analysis pass completed for `dreamplay-website-3`. Saved permanent high-level codebase context at `dreamplay-website-3/docs/REPO-CONTEXT.md`, including the key finding that the repo already has Supabase auth and a buyer decision portal, but does not yet have real Shopify Admin API order sync or true order-status integration.
- Turned the vague phrase "put these orders into the system" into a concrete Tier 1 implementation brief at `specs/dreamplay-orders-into-system-v1-tier1-spec.md`. Current Tier 1 recommendation: for V1, treat the imported local order snapshot as the canonical input, load it into the website's internal Supabase-backed order layer, and drive `/my-reservation` from that instead of requiring live Shopify access first.
# 2026-04-17

- Lionel provided a DreamPlay Shopify orders export and asked that it become the local canonical snapshot instead of relying on live Shopify access for now.
- Imported the raw export to `data/dreamplay/orders/imports/2026-04-16-shopify-export.csv` and created `data/dreamplay/orders/2026-04-16-orders-normalized.csv` plus `data/dreamplay/orders/2026-04-16-orders-summary.json` for assistant use.
- Snapshot summary: 56 orders, total `Total` paid amount = `$28118.37`, with 45 full payments, 5 half reservations, and 6 waitlist reservations.
- Durable DreamPlay business rule: use `Total` as actual customer paid, use `Lineitem name` to classify reservation vs full payment, and do not treat older orders as the current price sheet because prices were increased.
- Lionel then provided a canonical DreamPlay business-agent brief, now saved as `DREAMPLAY-TIER1-CONTEXT.md`.
- Key Tier 1 context captured: DreamPlay builds narrow-key digital pianos for smaller hands; DreamPlay 1 is $999 base / $1,899 Pro anchor pricing; there are 55 preorders and about $18K of preorder revenue reserved for prototype work, not operations; zero customers cancelled after the delivery delay notice.
- Important Tier 1 operating rules captured: customer portal and preorder updates are top priorities; refunds, pricing changes, Stripe issues, public delivery statements, customer complaints, and manufacturing decisions must be escalated to Lionel.
- Lionel clarified the role mapping explicitly: the main assistant is the DreamPlay Tier 1 bot, and the delegated worker is the Tier 2 bot.
- Current DreamPlay product task: customer portal for preorder buyers. Portal must let logged-in customers see their preorder status, amount paid, current selected choice, and three wired actions: Keep, Upgrade, Cancel.
- Tier 1 spec for this was created at `specs/dreamplay-customer-portal-v1-tier1-spec.md`. Current Tier 1 recommendation is that Upgrade and Cancel should be request/managed flows in V1 rather than fully automatic pricing/refund execution unless Lionel explicitly approves that.
- Repo analysis pass completed for `dreamplay-website-3`. Saved permanent high-level codebase context at `dreamplay-website-3/docs/REPO-CONTEXT.md`, including the key finding that the repo already has Supabase auth and a buyer decision portal, but does not yet have real Shopify Admin API order sync or true order-status integration.
- Turned the vague phrase "put these orders into the system" into a concrete Tier 1 implementation brief at `specs/dreamplay-orders-into-system-v1-tier1-spec.md`. Current Tier 1 recommendation: for V1, treat the imported local order snapshot as the canonical input, load it into the website's internal Supabase-backed order layer, and drive `/my-reservation` from that instead of requiring live Shopify access first.
- Lionel asked how memory works; current durable-memory framing is text files as source of truth with embeddings used only as a semantic recall layer.
- Lionel showed interest in switching memory embeddings from `text-embedding-3-small` to `text-embedding-3-large`; if adopted later, the memory index would need to be re-embedded/rebuilt.
- Saved the approved Tier 2 Claude Code worker brief at `specs/dreamplay-orders-into-system-v1-tier2-claude-prompt.md`.
- Current auth-gating finding for `dreamplay-website-3`: successful login alone is not enough for `/my-reservation`; the email must also exist in the `buyer_emails` table or the app redirects the user to `/vip`.
- Created a DreamPlay auth test user and verified the current flow locally: login succeeds, `/vip` loads, and `/my-reservation` redirects to `/vip` when the user is not in `buyer_emails`.
- Current blocker to fully testing `/my-reservation` with a fresh test account: the available Supabase key can create auth users but does not have permission to insert into `buyer_emails`.
- Lionel asked to try `lionel@musicalbasics.com` instead, since it is already allowlisted in `buyer_emails`.
- Verified locally with a created Supabase auth user for `lionel@musicalbasics.com`: login succeeds, `/vip` redirects to `/my-reservation`, and `/my-reservation` returns 200 and renders the reservation portal.
# 2026-04-17

- Lionel provided a DreamPlay Shopify orders export and asked that it become the local canonical snapshot instead of relying on live Shopify access for now.
- Imported the raw export to `data/dreamplay/orders/imports/2026-04-16-shopify-export.csv` and created `data/dreamplay/orders/2026-04-16-orders-normalized.csv` plus `data/dreamplay/orders/2026-04-16-orders-summary.json` for assistant use.
- Snapshot summary: 56 orders, total `Total` paid amount = `$28118.37`, with 45 full payments, 5 half reservations, and 6 waitlist reservations.
- Durable DreamPlay business rule: use `Total` as actual customer paid, use `Lineitem name` to classify reservation vs full payment, and do not treat older orders as the current price sheet because prices were increased.
- Lionel then provided a canonical DreamPlay business-agent brief, now saved as `DREAMPLAY-TIER1-CONTEXT.md`.
- Key Tier 1 context captured: DreamPlay builds narrow-key digital pianos for smaller hands; DreamPlay 1 is $999 base / $1,899 Pro anchor pricing; there are 55 preorders and about $18K of preorder revenue reserved for prototype work, not operations; zero customers cancelled after the delivery delay notice.
- Important Tier 1 operating rules captured: customer portal and preorder updates are top priorities; refunds, pricing changes, Stripe issues, public delivery statements, customer complaints, and manufacturing decisions must be escalated to Lionel.
- Lionel clarified the role mapping explicitly: the main assistant is the DreamPlay Tier 1 bot, and the delegated worker is the Tier 2 bot.
- Current DreamPlay product task: customer portal for preorder buyers. Portal must let logged-in customers see their preorder status, amount paid, current selected choice, and three wired actions: Keep, Upgrade, Cancel.
- Tier 1 spec for this was created at `specs/dreamplay-customer-portal-v1-tier1-spec.md`. Current Tier 1 recommendation is that Upgrade and Cancel should be request/managed flows in V1 rather than fully automatic pricing/refund execution unless Lionel explicitly approves that.
- Repo analysis pass completed for `dreamplay-website-3`. Saved permanent high-level codebase context at `dreamplay-website-3/docs/REPO-CONTEXT.md`, including the key finding that the repo already has Supabase auth and a buyer decision portal, but does not yet have real Shopify Admin API order sync or true order-status integration.
- Turned the vague phrase "put these orders into the system" into a concrete Tier 1 implementation brief at `specs/dreamplay-orders-into-system-v1-tier1-spec.md`. Current Tier 1 recommendation: for V1, treat the imported local order snapshot as the canonical input, load it into the website's internal Supabase-backed order layer, and drive `/my-reservation` from that instead of requiring live Shopify access first.
# 2026-04-17

- Lionel provided a DreamPlay Shopify orders export and asked that it become the local canonical snapshot instead of relying on live Shopify access for now.
- Imported the raw export to `data/dreamplay/orders/imports/2026-04-16-shopify-export.csv` and created `data/dreamplay/orders/2026-04-16-orders-normalized.csv` plus `data/dreamplay/orders/2026-04-16-orders-summary.json` for assistant use.
- Snapshot summary: 56 orders, total `Total` paid amount = `$28118.37`, with 45 full payments, 5 half reservations, and 6 waitlist reservations.
- Durable DreamPlay business rule: use `Total` as actual customer paid, use `Lineitem name` to classify reservation vs full payment, and do not treat older orders as the current price sheet because prices were increased.
- Lionel then provided a canonical DreamPlay business-agent brief, now saved as `DREAMPLAY-TIER1-CONTEXT.md`.
- Key Tier 1 context captured: DreamPlay builds narrow-key digital pianos for smaller hands; DreamPlay 1 is $999 base / $1,899 Pro anchor pricing; there are 55 preorders and about $18K of preorder revenue reserved for prototype work, not operations; zero customers cancelled after the delivery delay notice.
- Important Tier 1 operating rules captured: customer portal and preorder updates are top priorities; refunds, pricing changes, Stripe issues, public delivery statements, customer complaints, and manufacturing decisions must be escalated to Lionel.
- Lionel clarified the role mapping explicitly: the main assistant is the DreamPlay Tier 1 bot, and the delegated worker is the Tier 2 bot.
- Current DreamPlay product task: customer portal for preorder buyers. Portal must let logged-in customers see their preorder status, amount paid, current selected choice, and three wired actions: Keep, Upgrade, Cancel.
- Tier 1 spec for this was created at `specs/dreamplay-customer-portal-v1-tier1-spec.md`. Current Tier 1 recommendation is that Upgrade and Cancel should be request/managed flows in V1 rather than fully automatic pricing/refund execution unless Lionel explicitly approves that.
- Repo analysis pass completed for `dreamplay-website-3`. Saved permanent high-level codebase context at `dreamplay-website-3/docs/REPO-CONTEXT.md`, including the key finding that the repo already has Supabase auth and a buyer decision portal, but does not yet have real Shopify Admin API order sync or true order-status integration.
- Turned the vague phrase "put these orders into the system" into a concrete Tier 1 implementation brief at `specs/dreamplay-orders-into-system-v1-tier1-spec.md`. Current Tier 1 recommendation: for V1, treat the imported local order snapshot as the canonical input, load it into the website's internal Supabase-backed order layer, and drive `/my-reservation` from that instead of requiring live Shopify access first.
- Lionel asked how memory works; current durable-memory framing is text files as source of truth with embeddings used only as a semantic recall layer.
- Lionel showed interest in switching memory embeddings from `text-embedding-3-small` to `text-embedding-3-large`; if adopted later, the memory index would need to be re-embedded/rebuilt.
- Saved the approved Tier 2 Claude Code worker brief at `specs/dreamplay-orders-into-system-v1-tier2-claude-prompt.md`.
- Current auth-gating finding for `dreamplay-website-3`: successful login alone is not enough for `/my-reservation`; the email must also exist in the `buyer_emails` table or the app redirects the user to `/vip`.
- Created a DreamPlay auth test user and verified the current flow locally: login succeeds, `/vip` loads, and `/my-reservation` redirects to `/vip` when the user is not in `buyer_emails`.
- Current blocker to fully testing `/my-reservation` with a fresh test account: the available Supabase key can create auth users but does not have permission to insert into `buyer_emails`.
- Lionel asked to try `lionel@musicalbasics.com` instead, since it is already allowlisted in `buyer_emails`.
- Verified locally with a created Supabase auth user for `lionel@musicalbasics.com`: login succeeds, `/vip` redirects to `/my-reservation`, and `/my-reservation` returns 200 and renders the reservation portal.
- Lionel then hit a Vercel deploy failure for `dreamplay-website-3`; likely cause identified was missing or misnamed Supabase env vars in Vercel rather than a deeper code bug.
- The key env names the repo expects are `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, and `SUPABASE_SERVICE_ROLE_KEY` specifically, not `SUPABASE_SERVICE_KEY`.
- Build-time failure source found: `src/app/api/chat-session/route.ts` and `src/actions/admin-actions.ts` both create Supabase clients at module load, so `next build` can crash during route/page data collection when those env vars are absent.
- If the Vercel env issue persists, the next code-hardening fix would be to lazy-initialize those Supabase clients so missing envs fail later and more locally.
# 2026-04-17

- Lionel provided a DreamPlay Shopify orders export and asked that it become the local canonical snapshot instead of relying on live Shopify access for now.
- Imported the raw export to `data/dreamplay/orders/imports/2026-04-16-shopify-export.csv` and created `data/dreamplay/orders/2026-04-16-orders-normalized.csv` plus `data/dreamplay/orders/2026-04-16-orders-summary.json` for assistant use.
- Snapshot summary: 56 orders, total `Total` paid amount = `$28118.37`, with 45 full payments, 5 half reservations, and 6 waitlist reservations.
- Durable DreamPlay business rule: use `Total` as actual customer paid, use `Lineitem name` to classify reservation vs full payment, and do not treat older orders as the current price sheet because prices were increased.
- Lionel then provided a canonical DreamPlay business-agent brief, now saved as `DREAMPLAY-TIER1-CONTEXT.md`.
- Key Tier 1 context captured: DreamPlay builds narrow-key digital pianos for smaller hands; DreamPlay 1 is $999 base / $1,899 Pro anchor pricing; there are 55 preorders and about $18K of preorder revenue reserved for prototype work, not operations; zero customers cancelled after the delivery delay notice.
- Important Tier 1 operating rules captured: customer portal and preorder updates are top priorities; refunds, pricing changes, Stripe issues, public delivery statements, customer complaints, and manufacturing decisions must be escalated to Lionel.
- Lionel clarified the role mapping explicitly: the main assistant is the DreamPlay Tier 1 bot, and the delegated worker is the Tier 2 bot.
- Current DreamPlay product task: customer portal for preorder buyers. Portal must let logged-in customers see their preorder status, amount paid, current selected choice, and three wired actions: Keep, Upgrade, Cancel.
- Tier 1 spec for this was created at `specs/dreamplay-customer-portal-v1-tier1-spec.md`. Current Tier 1 recommendation is that Upgrade and Cancel should be request/managed flows in V1 rather than fully automatic pricing/refund execution unless Lionel explicitly approves that.
- Repo analysis pass completed for `dreamplay-website-3`. Saved permanent high-level codebase context at `dreamplay-website-3/docs/REPO-CONTEXT.md`, including the key finding that the repo already has Supabase auth and a buyer decision portal, but does not yet have real Shopify Admin API order sync or true order-status integration.
- Turned the vague phrase "put these orders into the system" into a concrete Tier 1 implementation brief at `specs/dreamplay-orders-into-system-v1-tier1-spec.md`. Current Tier 1 recommendation: for V1, treat the imported local order snapshot as the canonical input, load it into the website's internal Supabase-backed order layer, and drive `/my-reservation` from that instead of requiring live Shopify access first.
# 2026-04-17

- Lionel provided a DreamPlay Shopify orders export and asked that it become the local canonical snapshot instead of relying on live Shopify access for now.
- Imported the raw export to `data/dreamplay/orders/imports/2026-04-16-shopify-export.csv` and created `data/dreamplay/orders/2026-04-16-orders-normalized.csv` plus `data/dreamplay/orders/2026-04-16-orders-summary.json` for assistant use.
- Snapshot summary: 56 orders, total `Total` paid amount = `$28118.37`, with 45 full payments, 5 half reservations, and 6 waitlist reservations.
- Durable DreamPlay business rule: use `Total` as actual customer paid, use `Lineitem name` to classify reservation vs full payment, and do not treat older orders as the current price sheet because prices were increased.
- Lionel then provided a canonical DreamPlay business-agent brief, now saved as `DREAMPLAY-TIER1-CONTEXT.md`.
- Key Tier 1 context captured: DreamPlay builds narrow-key digital pianos for smaller hands; DreamPlay 1 is $999 base / $1,899 Pro anchor pricing; there are 55 preorders and about $18K of preorder revenue reserved for prototype work, not operations; zero customers cancelled after the delivery delay notice.
- Important Tier 1 operating rules captured: customer portal and preorder updates are top priorities; refunds, pricing changes, Stripe issues, public delivery statements, customer complaints, and manufacturing decisions must be escalated to Lionel.
- Lionel clarified the role mapping explicitly: the main assistant is the DreamPlay Tier 1 bot, and the delegated worker is the Tier 2 bot.
- Current DreamPlay product task: customer portal for preorder buyers. Portal must let logged-in customers see their preorder status, amount paid, current selected choice, and three wired actions: Keep, Upgrade, Cancel.
- Tier 1 spec for this was created at `specs/dreamplay-customer-portal-v1-tier1-spec.md`. Current Tier 1 recommendation is that Upgrade and Cancel should be request/managed flows in V1 rather than fully automatic pricing/refund execution unless Lionel explicitly approves that.
- Repo analysis pass completed for `dreamplay-website-3`. Saved permanent high-level codebase context at `dreamplay-website-3/docs/REPO-CONTEXT.md`, including the key finding that the repo already has Supabase auth and a buyer decision portal, but does not yet have real Shopify Admin API order sync or true order-status integration.
- Turned the vague phrase "put these orders into the system" into a concrete Tier 1 implementation brief at `specs/dreamplay-orders-into-system-v1-tier1-spec.md`. Current Tier 1 recommendation: for V1, treat the imported local order snapshot as the canonical input, load it into the website's internal Supabase-backed order layer, and drive `/my-reservation` from that instead of requiring live Shopify access first.
- Lionel asked how memory works; current durable-memory framing is text files as source of truth with embeddings used only as a semantic recall layer.
- Lionel showed interest in switching memory embeddings from `text-embedding-3-small` to `text-embedding-3-large`; if adopted later, the memory index would need to be re-embedded/rebuilt.
- Saved the approved Tier 2 Claude Code worker brief at `specs/dreamplay-orders-into-system-v1-tier2-claude-prompt.md`.
- Current auth-gating finding for `dreamplay-website-3`: successful login alone is not enough for `/my-reservation`; the email must also exist in the `buyer_emails` table or the app redirects the user to `/vip`.
- Created a DreamPlay auth test user and verified the current flow locally: login succeeds, `/vip` loads, and `/my-reservation` redirects to `/vip` when the user is not in `buyer_emails`.
- Current blocker to fully testing `/my-reservation` with a fresh test account: the available Supabase key can create auth users but does not have permission to insert into `buyer_emails`.
- Lionel asked to try `lionel@musicalbasics.com` instead, since it is already allowlisted in `buyer_emails`.
- Verified locally with a created Supabase auth user for `lionel@musicalbasics.com`: login succeeds, `/vip` redirects to `/my-reservation`, and `/my-reservation` returns 200 and renders the reservation portal.
- Lionel then hit a Vercel deploy failure for `dreamplay-website-3`; likely cause identified was missing or misnamed Supabase env vars in Vercel rather than a deeper code bug.
- The key env names the repo expects are `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, and `SUPABASE_SERVICE_ROLE_KEY` specifically, not `SUPABASE_SERVICE_KEY`.
- Build-time failure source found: `src/app/api/chat-session/route.ts` and `src/actions/admin-actions.ts` both create Supabase clients at module load, so `next build` can crash during route/page data collection when those env vars are absent.
- If the Vercel env issue persists, the next code-hardening fix would be to lazy-initialize those Supabase clients so missing envs fail later and more locally.
- Implemented the DreamPlay preorder order layer V1 in `dreamplay-website-3` and committed it as `4ee171e` with message `Implement preorder order layer v1`.
- The V1 implementation added `src/lib/preorders.ts`, `scripts/import-preorders.ts`, `scripts/verify-preorder-logic.ts`, preorder-related Supabase migrations under `supabase/migrations/`, and the implementation note `docs/preorder-order-layer-v1.md`, plus wiring updates in the reservation portal files.
- Important bug fix after the Claude pass: `saveReservationDecision` now writes the real `preorder_id` foreign-key column when available, while still falling back gracefully to `order_metadata` if the migration has not been applied yet.
- Verification status for the implementation: targeted lint passed after cleanup, `npx tsx scripts/verify-preorder-logic.ts` passed with `26 passed, 0 failed`, and a local Next build passed once the expected Supabase env vars were supplied in a gitignored `.env.local`.
- Lionel confirmed that the Vercel project already has the correct env variable names set. The working theory is now that the failed deployment was an env availability/timing issue on that deployment, so the next recommended step is a fresh redeploy before doing deeper code changes.
- Discord bot-mention troubleshooting note from `#general`: plain-text/name-style `@Openclaw 1` / `@Openclaw 2` references were not reliable enough for bot-to-bot ping confirmation. The observed reliable fix was to use raw Discord mentions directly, especially `<@1494663087373160580>` for Openclaw 1, `<@1494531954618536007>` for Openclaw 2, and `<@1494656974732656681>` for Openclaw 3. Once raw mentions were used in the same message, cross-bot visibility worked as expected in-channel.
- Discord special-role split was made explicit in Lionel's private server: Openclaw 1 is `dreamplay`, Openclaw 2 is `ultimate-pianist`, and Openclaw 3 is `concert-marketing`.
- Openclaw 1's current public self-name is `DreamPlay Bot`, and it should keep operating as the DreamPlay-focused / Tier 1 assistant.
- Lionel defined the multi-bot operating split explicitly: Openclaw Commander keeps high-level oversight and context, Openclaw 2 executes, Openclaw 3 handles concert marketing, and each bot should keep its role assignment noted in memory.
- Operational Discord note: Openclaw Commander could assign the matching special roles to the other bots, but could not assign itself `Commander` again because Discord returned `Missing Permissions`, likely due to role hierarchy / highest assignable position.

## [21:24] — DreamPlay portal live buyer test
* Information: Ran the first real authenticated `/my-reservation` check against the DreamPlay site using the safe existing buyer account `lionel@musicalbasics.com`.
* Information: Unauthenticated `/my-reservation` correctly redirected to `/login?next=/my-reservation`, and an authenticated buyer session returned HTTP 200 for `/my-reservation`.
* Information: Buyer gating worked through `isBuyer(...)` because `buyer_emails` is present in the target Supabase project.
* Information: Real decision persistence worked in Supabase for all three reservation decisions: `keep_reservation`, `upgrade_to_pro`, and `refund_requested`.
* Decision: Left the test account set back to `keep_reservation` after the live write checks.
* Error: Real preorder-backed summary data is still blocked in this environment because `public.preorder_orders` is missing from the Supabase schema cache, so the portal cannot yet show live `Preorder Status`, `Amount Paid`, `Payment Type`, `Current Reservation`, or `Reservation Reference` from imported preorder rows.
* Insight: The main live blocker is no longer auth or decision writes, but missing/populated preorder data in the intended Supabase environment.
* Task: If this Supabase project is the real live environment, apply `supabase/migrations/20260416000000_create_preorder_orders.sql` and run `scripts/import-preorders.ts`, or otherwise confirm which Supabase environment/schema is the source of truth for preorder data.
